% Options for packages loaded elsewhere
\PassOptionsToPackage{unicode}{hyperref}
\PassOptionsToPackage{hyphens}{url}
\documentclass[
]{article}
\usepackage{xcolor}
\usepackage[margin=1in]{geometry}
\usepackage{amsmath,amssymb}
\setcounter{secnumdepth}{-\maxdimen} % remove section numbering
\usepackage{iftex}
\ifPDFTeX
  \usepackage[T1]{fontenc}
  \usepackage[utf8]{inputenc}
  \usepackage{textcomp} % provide euro and other symbols
\else % if luatex or xetex
  \usepackage{unicode-math} % this also loads fontspec
  \defaultfontfeatures{Scale=MatchLowercase}
  \defaultfontfeatures[\rmfamily]{Ligatures=TeX,Scale=1}
\fi
\usepackage{lmodern}
\ifPDFTeX\else
  % xetex/luatex font selection
\fi
% Use upquote if available, for straight quotes in verbatim environments
\IfFileExists{upquote.sty}{\usepackage{upquote}}{}
\IfFileExists{microtype.sty}{% use microtype if available
  \usepackage[]{microtype}
  \UseMicrotypeSet[protrusion]{basicmath} % disable protrusion for tt fonts
}{}
\makeatletter
\@ifundefined{KOMAClassName}{% if non-KOMA class
  \IfFileExists{parskip.sty}{%
    \usepackage{parskip}
  }{% else
    \setlength{\parindent}{0pt}
    \setlength{\parskip}{6pt plus 2pt minus 1pt}}
}{% if KOMA class
  \KOMAoptions{parskip=half}}
\makeatother
\usepackage{graphicx}
\makeatletter
\newsavebox\pandoc@box
\newcommand*\pandocbounded[1]{% scales image to fit in text height/width
  \sbox\pandoc@box{#1}%
  \Gscale@div\@tempa{\textheight}{\dimexpr\ht\pandoc@box+\dp\pandoc@box\relax}%
  \Gscale@div\@tempb{\linewidth}{\wd\pandoc@box}%
  \ifdim\@tempb\p@<\@tempa\p@\let\@tempa\@tempb\fi% select the smaller of both
  \ifdim\@tempa\p@<\p@\scalebox{\@tempa}{\usebox\pandoc@box}%
  \else\usebox{\pandoc@box}%
  \fi%
}
% Set default figure placement to htbp
\def\fps@figure{htbp}
\makeatother
\setlength{\emergencystretch}{3em} % prevent overfull lines
\providecommand{\tightlist}{%
  \setlength{\itemsep}{0pt}\setlength{\parskip}{0pt}}
\usepackage{bookmark}
\IfFileExists{xurl.sty}{\usepackage{xurl}}{} % add URL line breaks if available
\urlstyle{same}
\hypersetup{
  pdftitle={PRISMA --- systematic reviews \& meta-analyses},
  hidelinks,
  pdfcreator={LaTeX via pandoc}}

\title{PRISMA --- systematic reviews \& meta-analyses}
\author{}
\date{\vspace{-2.5em}}

\begin{document}
\maketitle

\section{PRISMA --- systematic reviews \&
meta-analyses}\label{prisma-systematic-reviews-meta-analyses}

PRISMA --- \emph{Preferred Reporting Items for Systematic reviews and
Meta-Analyses} --- covers systematic reviews and meta-analyses. The
27-item checklist and flow diagram are at
\url{https://www.prisma-statement.org/}. The templates below cover
pairwise and network meta-analysis methods.

This page contains \textbf{25 method-specific templates}. Each is a
skeleton with placeholders in italics; the prose is written so you can
paste it into a manuscript and replace only the bracketed values.

\subsubsection{Bivariate SROC Curves}\label{bivariate-sroc-curves}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Bivariate SROC Curves} as implemented in \emph{{[}package
vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list
checks performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Bivariate SROC Curves yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/15-metaanalysis/bivariate-sroc.Rmd\#for-reviewers}{\texttt{bivariate-sroc}
--- For Reviewers}

\subsubsection{Converting Between Effect
Sizes}\label{converting-between-effect-sizes}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Converting Between Effect Sizes} as implemented in
\emph{{[}package vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were
checked: \emph{{[}list checks performed{]}}. \emph{{[}Effect size with
95\% confidence interval reported as the primary outcome{]}}. Two-sided
p-values use \emph{α = 0.05}; \emph{{[}multiple-comparison adjustment if
any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Converting Between Effect Sizes yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/15-metaanalysis/effect-size-conversion.Rmd\#for-reviewers}{\texttt{effect-size-conversion}
--- For Reviewers}

\subsubsection{Cumulative Meta-Analysis}\label{cumulative-meta-analysis}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Cumulative Meta-Analysis} as implemented in \emph{{[}package
vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list
checks performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Cumulative Meta-Analysis yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/15-metaanalysis/cumulative-meta-analysis.Rmd\#for-reviewers}{\texttt{cumulative-meta-analysis}
--- For Reviewers}

\subsubsection{Egger's Test for Funnel
Asymmetry}\label{eggers-test-for-funnel-asymmetry}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Egger's Test for Funnel Asymmetry} as implemented in
\emph{{[}package vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were
checked: \emph{{[}list checks performed{]}}. \emph{{[}Effect size with
95\% confidence interval reported as the primary outcome{]}}. Two-sided
p-values use \emph{α = 0.05}; \emph{{[}multiple-comparison adjustment if
any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Egger's Test for Funnel Asymmetry yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/15-metaanalysis/egger-test.Rmd\#for-reviewers}{\texttt{egger-test}
--- For Reviewers}

\subsubsection{Fixed-Effect vs.~Random-Effects
Meta-Analysis}\label{fixed-effect-vs.-random-effects-meta-analysis}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Fixed-Effect vs.~Random-Effects Meta-Analysis} as implemented in
\emph{{[}package vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were
checked: \emph{{[}list checks performed{]}}. \emph{{[}Effect size with
95\% confidence interval reported as the primary outcome{]}}. Two-sided
p-values use \emph{α = 0.05}; \emph{{[}multiple-comparison adjustment if
any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Fixed-Effect vs.~Random-Effects Meta-Analysis yielded
\emph{{[}estimate{]}} (95\% CI \emph{{[}lower, upper{]}}, p =
\emph{{[}value{]}}). \emph{{[}Diagnostic / assumption summary{]}}.
\emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/15-metaanalysis/fixed-vs-random-effects.Rmd\#for-reviewers}{\texttt{fixed-vs-random-effects}
--- For Reviewers}

\subsubsection{Forest Plots in
Meta-Analysis}\label{forest-plots-in-meta-analysis}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Forest Plots in Meta-Analysis} as implemented in
\emph{{[}package vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were
checked: \emph{{[}list checks performed{]}}. \emph{{[}Effect size with
95\% confidence interval reported as the primary outcome{]}}. Two-sided
p-values use \emph{α = 0.05}; \emph{{[}multiple-comparison adjustment if
any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Forest Plots in Meta-Analysis yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/15-metaanalysis/forest-plot-meta.Rmd\#for-reviewers}{\texttt{forest-plot-meta}
--- For Reviewers}

\subsubsection{Funnel Plots}\label{funnel-plots}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Funnel Plots} as implemented in \emph{{[}package vX.Y{]}} in R
\emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list checks
performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Funnel Plots yielded \emph{{[}estimate{]}} (95\% CI \emph{{[}lower,
upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic / assumption
summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/15-metaanalysis/funnel-plot-meta.Rmd\#for-reviewers}{\texttt{funnel-plot-meta}
--- For Reviewers}

\subsubsection{Individual Participant Data
Meta-Analysis}\label{individual-participant-data-meta-analysis}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Individual Participant Data Meta-Analysis} as implemented in
\emph{{[}package vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were
checked: \emph{{[}list checks performed{]}}. \emph{{[}Effect size with
95\% confidence interval reported as the primary outcome{]}}. Two-sided
p-values use \emph{α = 0.05}; \emph{{[}multiple-comparison adjustment if
any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Individual Participant Data Meta-Analysis yielded \emph{{[}estimate{]}}
(95\% CI \emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}).
\emph{{[}Diagnostic / assumption summary{]}}. \emph{{[}Sensitivity
analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/15-metaanalysis/ipd-meta-analysis.Rmd\#for-reviewers}{\texttt{ipd-meta-analysis}
--- For Reviewers}

\subsubsection{Leave-One-Out
Meta-Analysis}\label{leave-one-out-meta-analysis}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Leave-One-Out Meta-Analysis} as implemented in \emph{{[}package
vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list
checks performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Leave-One-Out Meta-Analysis yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/15-metaanalysis/leave-one-out.Rmd\#for-reviewers}{\texttt{leave-one-out}
--- For Reviewers}

\subsubsection{Meta-Analysis of Diagnostic
Accuracy}\label{meta-analysis-of-diagnostic-accuracy}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Meta-Analysis of Diagnostic Accuracy} as implemented in
\emph{{[}package vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were
checked: \emph{{[}list checks performed{]}}. \emph{{[}Effect size with
95\% confidence interval reported as the primary outcome{]}}. Two-sided
p-values use \emph{α = 0.05}; \emph{{[}multiple-comparison adjustment if
any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Meta-Analysis of Diagnostic Accuracy yielded \emph{{[}estimate{]}} (95\%
CI \emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}).
\emph{{[}Diagnostic / assumption summary{]}}. \emph{{[}Sensitivity
analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/15-metaanalysis/diagnostic-test-meta.Rmd\#for-reviewers}{\texttt{diagnostic-test-meta}
--- For Reviewers}

\subsubsection{Meta-Regression}\label{meta-regression}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Meta-Regression} as implemented in \emph{{[}package vX.Y{]}} in
R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list checks
performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Meta-Regression yielded \emph{{[}estimate{]}} (95\% CI \emph{{[}lower,
upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic / assumption
summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/15-metaanalysis/meta-regression.Rmd\#for-reviewers}{\texttt{meta-regression}
--- For Reviewers}

\subsubsection{Network Meta-Analysis:
Introduction}\label{network-meta-analysis-introduction}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Network Meta-Analysis: Introduction} as implemented in
\emph{{[}package vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were
checked: \emph{{[}list checks performed{]}}. \emph{{[}Effect size with
95\% confidence interval reported as the primary outcome{]}}. Two-sided
p-values use \emph{α = 0.05}; \emph{{[}multiple-comparison adjustment if
any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Network Meta-Analysis: Introduction yielded \emph{{[}estimate{]}} (95\%
CI \emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}).
\emph{{[}Diagnostic / assumption summary{]}}. \emph{{[}Sensitivity
analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/15-metaanalysis/network-meta-analysis-intro.Rmd\#for-reviewers}{\texttt{network-meta-analysis-intro}
--- For Reviewers}

\subsubsection{NMA Consistency and
Inconsistency}\label{nma-consistency-and-inconsistency}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{NMA Consistency and Inconsistency} as implemented in
\emph{{[}package vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were
checked: \emph{{[}list checks performed{]}}. \emph{{[}Effect size with
95\% confidence interval reported as the primary outcome{]}}. Two-sided
p-values use \emph{α = 0.05}; \emph{{[}multiple-comparison adjustment if
any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
NMA Consistency and Inconsistency yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/15-metaanalysis/nma-consistency.Rmd\#for-reviewers}{\texttt{nma-consistency}
--- For Reviewers}

\subsubsection{NMA League Tables}\label{nma-league-tables}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{NMA League Tables} as implemented in \emph{{[}package vX.Y{]}}
in R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list checks
performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
NMA League Tables yielded \emph{{[}estimate{]}} (95\% CI \emph{{[}lower,
upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic / assumption
summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/15-metaanalysis/nma-league-tables.Rmd\#for-reviewers}{\texttt{nma-league-tables}
--- For Reviewers}

\subsubsection{Pooling Correlation
Coefficients}\label{pooling-correlation-coefficients}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Pooling Correlation Coefficients} as implemented in
\emph{{[}package vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were
checked: \emph{{[}list checks performed{]}}. \emph{{[}Effect size with
95\% confidence interval reported as the primary outcome{]}}. Two-sided
p-values use \emph{α = 0.05}; \emph{{[}multiple-comparison adjustment if
any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Pooling Correlation Coefficients yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/15-metaanalysis/correlation-pooling.Rmd\#for-reviewers}{\texttt{correlation-pooling}
--- For Reviewers}

\subsubsection{Pooling Log-Odds Ratios}\label{pooling-log-odds-ratios}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Pooling Log-Odds Ratios} as implemented in \emph{{[}package
vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list
checks performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Pooling Log-Odds Ratios yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/15-metaanalysis/log-odds-ratio-pooling.Rmd\#for-reviewers}{\texttt{log-odds-ratio-pooling}
--- For Reviewers}

\subsubsection{Pooling Risk Ratios}\label{pooling-risk-ratios}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Pooling Risk Ratios} as implemented in \emph{{[}package vX.Y{]}}
in R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list checks
performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Pooling Risk Ratios yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/15-metaanalysis/risk-ratio-pooling.Rmd\#for-reviewers}{\texttt{risk-ratio-pooling}
--- For Reviewers}

\subsubsection{Prediction Intervals in
Meta-Analysis}\label{prediction-intervals-in-meta-analysis}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Prediction Intervals in Meta-Analysis} as implemented in
\emph{{[}package vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were
checked: \emph{{[}list checks performed{]}}. \emph{{[}Effect size with
95\% confidence interval reported as the primary outcome{]}}. Two-sided
p-values use \emph{α = 0.05}; \emph{{[}multiple-comparison adjustment if
any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Prediction Intervals in Meta-Analysis yielded \emph{{[}estimate{]}}
(95\% CI \emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}).
\emph{{[}Diagnostic / assumption summary{]}}. \emph{{[}Sensitivity
analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/15-metaanalysis/prediction-interval-meta.Rmd\#for-reviewers}{\texttt{prediction-interval-meta}
--- For Reviewers}

\subsubsection{Quantifying Heterogeneity: Q and
I\^{}2}\label{quantifying-heterogeneity-q-and-i2}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Quantifying Heterogeneity: Q and I\^{}2} as implemented in
\emph{{[}package vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were
checked: \emph{{[}list checks performed{]}}. \emph{{[}Effect size with
95\% confidence interval reported as the primary outcome{]}}. Two-sided
p-values use \emph{α = 0.05}; \emph{{[}multiple-comparison adjustment if
any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Quantifying Heterogeneity: Q and I\^{}2 yielded \emph{{[}estimate{]}}
(95\% CI \emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}).
\emph{{[}Diagnostic / assumption summary{]}}. \emph{{[}Sensitivity
analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/15-metaanalysis/heterogeneity-q-i2.Rmd\#for-reviewers}{\texttt{heterogeneity-q-i2}
--- For Reviewers}

\subsubsection{Selection Models for Publication
Bias}\label{selection-models-for-publication-bias}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Selection Models for Publication Bias} as implemented in
\emph{{[}package vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were
checked: \emph{{[}list checks performed{]}}. \emph{{[}Effect size with
95\% confidence interval reported as the primary outcome{]}}. Two-sided
p-values use \emph{α = 0.05}; \emph{{[}multiple-comparison adjustment if
any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Selection Models for Publication Bias yielded \emph{{[}estimate{]}}
(95\% CI \emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}).
\emph{{[}Diagnostic / assumption summary{]}}. \emph{{[}Sensitivity
analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/15-metaanalysis/selection-models-bias.Rmd\#for-reviewers}{\texttt{selection-models-bias}
--- For Reviewers}

\subsubsection{Standardised Mean Difference: Hedges'
g}\label{standardised-mean-difference-hedges-g}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Standardised Mean Difference: Hedges' g} as implemented in
\emph{{[}package vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were
checked: \emph{{[}list checks performed{]}}. \emph{{[}Effect size with
95\% confidence interval reported as the primary outcome{]}}. Two-sided
p-values use \emph{α = 0.05}; \emph{{[}multiple-comparison adjustment if
any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Standardised Mean Difference: Hedges' g yielded \emph{{[}estimate{]}}
(95\% CI \emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}).
\emph{{[}Diagnostic / assumption summary{]}}. \emph{{[}Sensitivity
analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/15-metaanalysis/smd-hedges-g.Rmd\#for-reviewers}{\texttt{smd-hedges-g}
--- For Reviewers}

\subsubsection{Subgroup Meta-Analysis}\label{subgroup-meta-analysis}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Subgroup Meta-Analysis} as implemented in \emph{{[}package
vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list
checks performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Subgroup Meta-Analysis yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/15-metaanalysis/subgroup-meta-analysis.Rmd\#for-reviewers}{\texttt{subgroup-meta-analysis}
--- For Reviewers}

\subsubsection{SUCRA Rankings}\label{sucra-rankings}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{SUCRA Rankings} as implemented in \emph{{[}package vX.Y{]}} in R
\emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list checks
performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
SUCRA Rankings yielded \emph{{[}estimate{]}} (95\% CI \emph{{[}lower,
upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic / assumption
summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/15-metaanalysis/nma-sucra.Rmd\#for-reviewers}{\texttt{nma-sucra}
--- For Reviewers}

\subsubsection{Tau-Squared Estimation}\label{tau-squared-estimation}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Tau-Squared Estimation} as implemented in \emph{{[}package
vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list
checks performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Tau-Squared Estimation yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/15-metaanalysis/tau-squared-estimation.Rmd\#for-reviewers}{\texttt{tau-squared-estimation}
--- For Reviewers}

\subsubsection{Trim-and-Fill}\label{trim-and-fill}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Trim-and-Fill} as implemented in \emph{{[}package vX.Y{]}} in R
\emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list checks
performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Trim-and-Fill yielded \emph{{[}estimate{]}} (95\% CI \emph{{[}lower,
upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic / assumption
summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/15-metaanalysis/trim-and-fill.Rmd\#for-reviewers}{\texttt{trim-and-fill}
--- For Reviewers}

\end{document}
