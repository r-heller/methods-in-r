% Options for packages loaded elsewhere
\PassOptionsToPackage{unicode}{hyperref}
\PassOptionsToPackage{hyphens}{url}
\documentclass[
]{article}
\usepackage{xcolor}
\usepackage[margin=1in]{geometry}
\usepackage{amsmath,amssymb}
\setcounter{secnumdepth}{-\maxdimen} % remove section numbering
\usepackage{iftex}
\ifPDFTeX
  \usepackage[T1]{fontenc}
  \usepackage[utf8]{inputenc}
  \usepackage{textcomp} % provide euro and other symbols
\else % if luatex or xetex
  \usepackage{unicode-math} % this also loads fontspec
  \defaultfontfeatures{Scale=MatchLowercase}
  \defaultfontfeatures[\rmfamily]{Ligatures=TeX,Scale=1}
\fi
\usepackage{lmodern}
\ifPDFTeX\else
  % xetex/luatex font selection
\fi
% Use upquote if available, for straight quotes in verbatim environments
\IfFileExists{upquote.sty}{\usepackage{upquote}}{}
\IfFileExists{microtype.sty}{% use microtype if available
  \usepackage[]{microtype}
  \UseMicrotypeSet[protrusion]{basicmath} % disable protrusion for tt fonts
}{}
\makeatletter
\@ifundefined{KOMAClassName}{% if non-KOMA class
  \IfFileExists{parskip.sty}{%
    \usepackage{parskip}
  }{% else
    \setlength{\parindent}{0pt}
    \setlength{\parskip}{6pt plus 2pt minus 1pt}}
}{% if KOMA class
  \KOMAoptions{parskip=half}}
\makeatother
\usepackage{graphicx}
\makeatletter
\newsavebox\pandoc@box
\newcommand*\pandocbounded[1]{% scales image to fit in text height/width
  \sbox\pandoc@box{#1}%
  \Gscale@div\@tempa{\textheight}{\dimexpr\ht\pandoc@box+\dp\pandoc@box\relax}%
  \Gscale@div\@tempb{\linewidth}{\wd\pandoc@box}%
  \ifdim\@tempb\p@<\@tempa\p@\let\@tempa\@tempb\fi% select the smaller of both
  \ifdim\@tempa\p@<\p@\scalebox{\@tempa}{\usebox\pandoc@box}%
  \else\usebox{\pandoc@box}%
  \fi%
}
% Set default figure placement to htbp
\def\fps@figure{htbp}
\makeatother
\setlength{\emergencystretch}{3em} % prevent overfull lines
\providecommand{\tightlist}{%
  \setlength{\itemsep}{0pt}\setlength{\parskip}{0pt}}
\usepackage{bookmark}
\IfFileExists{xurl.sty}{\usepackage{xurl}}{} % add URL line breaks if available
\urlstyle{same}
\hypersetup{
  pdftitle={TRIPOD-AI --- prediction models},
  hidelinks,
  pdfcreator={LaTeX via pandoc}}

\title{TRIPOD-AI --- prediction models}
\author{}
\date{\vspace{-2.5em}}

\begin{document}
\maketitle

\section{TRIPOD-AI --- prediction
models}\label{tripod-ai-prediction-models}

TRIPOD-AI --- \emph{Transparent Reporting of a multivariable prediction
model for Individual Prognosis Or Diagnosis, AI extension} --- covers
prediction models, including those built with machine learning. The
checklist is at \url{https://www.tripod-statement.org/}. The templates
below cover model development, validation, calibration, and fairness
reporting.

This page contains \textbf{163 method-specific templates}. Each is a
skeleton with placeholders in italics; the prose is written so you can
paste it into a manuscript and replace only the bracketed values.

\subsubsection{A Complete mlr3 Workflow}\label{a-complete-mlr3-workflow}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using \textbf{A
Complete mlr3 Workflow} as implemented in \emph{{[}package vX.Y{]}} in R
\emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list checks
performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
A Complete mlr3 Workflow yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/13-ml/mlr3-workflow.Rmd\#for-reviewers}{\texttt{mlr3-workflow}
--- For Reviewers}

\subsubsection{A Complete tidymodels
Workflow}\label{a-complete-tidymodels-workflow}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using \textbf{A
Complete tidymodels Workflow} as implemented in \emph{{[}package
vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list
checks performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
A Complete tidymodels Workflow yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/13-ml/tidymodels-workflow.Rmd\#for-reviewers}{\texttt{tidymodels-workflow}
--- For Reviewers}

\subsubsection{Accelerated Failure Time
Models}\label{accelerated-failure-time-models}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Accelerated Failure Time Models} as implemented in
\emph{{[}package vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were
checked: \emph{{[}list checks performed{]}}. \emph{{[}Effect size with
95\% confidence interval reported as the primary outcome{]}}. Two-sided
p-values use \emph{α = 0.05}; \emph{{[}multiple-comparison adjustment if
any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Accelerated Failure Time Models yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/11-survival/accelerated-failure-time.Rmd\#for-reviewers}{\texttt{accelerated-failure-time}
--- For Reviewers}

\subsubsection{Adaptive Trial Designs}\label{adaptive-trial-designs}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Adaptive Trial Designs} as implemented in \emph{{[}package
vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list
checks performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Adaptive Trial Designs yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/14-clinical/adaptive-design.Rmd\#for-reviewers}{\texttt{adaptive-design}
--- For Reviewers}

\subsubsection{Added-Variable Plots}\label{added-variable-plots}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Added-Variable Plots} as implemented in \emph{{[}package
vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list
checks performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Added-Variable Plots yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/07-regression/added-variable-plots.Rmd\#for-reviewers}{\texttt{added-variable-plots}
--- For Reviewers}

\subsubsection{Alpha-Spending Functions}\label{alpha-spending-functions}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Alpha-Spending Functions} as implemented in \emph{{[}package
vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list
checks performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Alpha-Spending Functions yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/14-clinical/alpha-spending.Rmd\#for-reviewers}{\texttt{alpha-spending}
--- For Reviewers}

\subsubsection{Anomaly Detection}\label{anomaly-detection}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Anomaly Detection} as implemented in \emph{{[}package vX.Y{]}}
in R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list checks
performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Anomaly Detection yielded \emph{{[}estimate{]}} (95\% CI \emph{{[}lower,
upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic / assumption
summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/13-ml/anomaly-detection.Rmd\#for-reviewers}{\texttt{anomaly-detection}
--- For Reviewers}

\subsubsection{Bagging}\label{bagging}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Bagging} as implemented in \emph{{[}package vX.Y{]}} in R
\emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list checks
performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Bagging yielded \emph{{[}estimate{]}} (95\% CI \emph{{[}lower,
upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic / assumption
summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/13-ml/bagging.Rmd\#for-reviewers}{\texttt{bagging} ---
For Reviewers}

\subsubsection{Baseline Adjustment with
ANCOVA}\label{baseline-adjustment-with-ancova}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Baseline Adjustment with ANCOVA} as implemented in
\emph{{[}package vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were
checked: \emph{{[}list checks performed{]}}. \emph{{[}Effect size with
95\% confidence interval reported as the primary outcome{]}}. Two-sided
p-values use \emph{α = 0.05}; \emph{{[}multiple-comparison adjustment if
any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Baseline Adjustment with ANCOVA yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/14-clinical/baseline-adjustment-ancova.Rmd\#for-reviewers}{\texttt{baseline-adjustment-ancova}
--- For Reviewers}

\subsubsection{Best Subset Selection}\label{best-subset-selection}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Best Subset Selection} as implemented in \emph{{[}package
vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list
checks performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Best Subset Selection yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/07-regression/best-subset-selection.Rmd\#for-reviewers}{\texttt{best-subset-selection}
--- For Reviewers}

\subsubsection{Beta Regression}\label{beta-regression}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Beta Regression} as implemented in \emph{{[}package vX.Y{]}} in
R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list checks
performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Beta Regression yielded \emph{{[}estimate{]}} (95\% CI \emph{{[}lower,
upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic / assumption
summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/07-regression/beta-regression.Rmd\#for-reviewers}{\texttt{beta-regression}
--- For Reviewers}

\subsubsection{Bland-Altman Limits of
Agreement}\label{bland-altman-limits-of-agreement}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Bland-Altman Limits of Agreement} as implemented in
\emph{{[}package vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were
checked: \emph{{[}list checks performed{]}}. \emph{{[}Effect size with
95\% confidence interval reported as the primary outcome{]}}. Two-sided
p-values use \emph{α = 0.05}; \emph{{[}multiple-comparison adjustment if
any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Bland-Altman Limits of Agreement yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/14-clinical/bland-altman-analysis.Rmd\#for-reviewers}{\texttt{bland-altman-analysis}
--- For Reviewers}

\subsubsection{Blinding Procedures}\label{blinding-procedures}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Blinding Procedures} as implemented in \emph{{[}package vX.Y{]}}
in R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list checks
performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Blinding Procedures yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/14-clinical/blinding-procedures.Rmd\#for-reviewers}{\texttt{blinding-procedures}
--- For Reviewers}

\subsubsection{Block Randomisation}\label{block-randomisation}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Block Randomisation} as implemented in \emph{{[}package vX.Y{]}}
in R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list checks
performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Block Randomisation yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/14-clinical/block-randomization.Rmd\#for-reviewers}{\texttt{block-randomization}
--- For Reviewers}

\subsubsection{Calibration Plots}\label{calibration-plots}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Calibration Plots} as implemented in \emph{{[}package vX.Y{]}}
in R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list checks
performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Calibration Plots yielded \emph{{[}estimate{]}} (95\% CI \emph{{[}lower,
upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic / assumption
summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/13-ml/calibration-plots.Rmd\#for-reviewers}{\texttt{calibration-plots}
--- For Reviewers}

\subsubsection{CatBoost}\label{catboost}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{CatBoost} as implemented in \emph{{[}package vX.Y{]}} in R
\emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list checks
performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
CatBoost yielded \emph{{[}estimate{]}} (95\% CI \emph{{[}lower,
upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic / assumption
summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/13-ml/catboost.Rmd\#for-reviewers}{\texttt{catboost}
--- For Reviewers}

\subsubsection{Censoring Types}\label{censoring-types}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Censoring Types} as implemented in \emph{{[}package vX.Y{]}} in
R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list checks
performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Censoring Types yielded \emph{{[}estimate{]}} (95\% CI \emph{{[}lower,
upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic / assumption
summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/11-survival/censoring-types.Rmd\#for-reviewers}{\texttt{censoring-types}
--- For Reviewers}

\subsubsection{Centring and Scaling
Predictors}\label{centring-and-scaling-predictors}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Centring and Scaling Predictors} as implemented in
\emph{{[}package vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were
checked: \emph{{[}list checks performed{]}}. \emph{{[}Effect size with
95\% confidence interval reported as the primary outcome{]}}. Two-sided
p-values use \emph{α = 0.05}; \emph{{[}multiple-comparison adjustment if
any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Centring and Scaling Predictors yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/07-regression/centered-scaled-predictors.Rmd\#for-reviewers}{\texttt{centered-scaled-predictors}
--- For Reviewers}

\subsubsection{Checking Cox Assumptions}\label{checking-cox-assumptions}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Checking Cox Assumptions} as implemented in \emph{{[}package
vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list
checks performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Checking Cox Assumptions yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/11-survival/cox-assumptions-check.Rmd\#for-reviewers}{\texttt{cox-assumptions-check}
--- For Reviewers}

\subsubsection{Class Weights for Imbalanced
Data}\label{class-weights-for-imbalanced-data}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Class Weights for Imbalanced Data} as implemented in
\emph{{[}package vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were
checked: \emph{{[}list checks performed{]}}. \emph{{[}Effect size with
95\% confidence interval reported as the primary outcome{]}}. Two-sided
p-values use \emph{α = 0.05}; \emph{{[}multiple-comparison adjustment if
any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Class Weights for Imbalanced Data yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/13-ml/class-weights-imbalanced.Rmd\#for-reviewers}{\texttt{class-weights-imbalanced}
--- For Reviewers}

\subsubsection{Clinical Equivalence
Trials}\label{clinical-equivalence-trials}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Clinical Equivalence Trials} as implemented in \emph{{[}package
vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list
checks performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Clinical Equivalence Trials yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/14-clinical/equivalence-clinical.Rmd\#for-reviewers}{\texttt{equivalence-clinical}
--- For Reviewers}

\subsubsection{Cluster-Randomised
Trials}\label{cluster-randomised-trials}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Cluster-Randomised Trials} as implemented in \emph{{[}package
vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list
checks performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Cluster-Randomised Trials yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/14-clinical/cluster-rct.Rmd\#for-reviewers}{\texttt{cluster-rct}
--- For Reviewers}

\subsubsection{Cohen's Kappa}\label{cohens-kappa}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Cohen's Kappa} as implemented in \emph{{[}package vX.Y{]}} in R
\emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list checks
performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Cohen's Kappa yielded \emph{{[}estimate{]}} (95\% CI \emph{{[}lower,
upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic / assumption
summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/14-clinical/cohens-kappa.Rmd\#for-reviewers}{\texttt{cohens-kappa}
--- For Reviewers}

\subsubsection{Competing Risks: Cumulative
Incidence}\label{competing-risks-cumulative-incidence}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Competing Risks: Cumulative Incidence} as implemented in
\emph{{[}package vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were
checked: \emph{{[}list checks performed{]}}. \emph{{[}Effect size with
95\% confidence interval reported as the primary outcome{]}}. Two-sided
p-values use \emph{α = 0.05}; \emph{{[}multiple-comparison adjustment if
any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Competing Risks: Cumulative Incidence yielded \emph{{[}estimate{]}}
(95\% CI \emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}).
\emph{{[}Diagnostic / assumption summary{]}}. \emph{{[}Sensitivity
analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/11-survival/competing-risks-cif.Rmd\#for-reviewers}{\texttt{competing-risks-cif}
--- For Reviewers}

\subsubsection{Concordance and the
C-Index}\label{concordance-and-the-c-index}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Concordance and the C-Index} as implemented in \emph{{[}package
vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list
checks performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Concordance and the C-Index yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/11-survival/concordance-c-index.Rmd\#for-reviewers}{\texttt{concordance-c-index}
--- For Reviewers}

\subsubsection{Conditional Power}\label{conditional-power}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Conditional Power} as implemented in \emph{{[}package vX.Y{]}}
in R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list checks
performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Conditional Power yielded \emph{{[}estimate{]}} (95\% CI \emph{{[}lower,
upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic / assumption
summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/14-clinical/conditional-power.Rmd\#for-reviewers}{\texttt{conditional-power}
--- For Reviewers}

\subsubsection{Conditional Survival}\label{conditional-survival}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Conditional Survival} as implemented in \emph{{[}package
vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list
checks performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Conditional Survival yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/11-survival/conditional-survival.Rmd\#for-reviewers}{\texttt{conditional-survival}
--- For Reviewers}

\subsubsection{Contrasts in R}\label{contrasts-in-r}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Contrasts in R} as implemented in \emph{{[}package vX.Y{]}} in R
\emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list checks
performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Contrasts in R yielded \emph{{[}estimate{]}} (95\% CI \emph{{[}lower,
upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic / assumption
summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/07-regression/contrasts-in-r.Rmd\#for-reviewers}{\texttt{contrasts-in-r}
--- For Reviewers}

\subsubsection{Convolutional Neural Networks:
Introduction}\label{convolutional-neural-networks-introduction}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Convolutional Neural Networks: Introduction} as implemented in
\emph{{[}package vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were
checked: \emph{{[}list checks performed{]}}. \emph{{[}Effect size with
95\% confidence interval reported as the primary outcome{]}}. Two-sided
p-values use \emph{α = 0.05}; \emph{{[}multiple-comparison adjustment if
any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Convolutional Neural Networks: Introduction yielded
\emph{{[}estimate{]}} (95\% CI \emph{{[}lower, upper{]}}, p =
\emph{{[}value{]}}). \emph{{[}Diagnostic / assumption summary{]}}.
\emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/13-ml/cnn-introduction.Rmd\#for-reviewers}{\texttt{cnn-introduction}
--- For Reviewers}

\subsubsection{Cook's Distance}\label{cooks-distance}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Cook's Distance} as implemented in \emph{{[}package vX.Y{]}} in
R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list checks
performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Cook's Distance yielded \emph{{[}estimate{]}} (95\% CI \emph{{[}lower,
upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic / assumption
summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/07-regression/cooks-distance.Rmd\#for-reviewers}{\texttt{cooks-distance}
--- For Reviewers}

\subsubsection{Cox Proportional Hazards
Regression}\label{cox-proportional-hazards-regression}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Cox Proportional Hazards Regression} as implemented in
\emph{{[}package vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were
checked: \emph{{[}list checks performed{]}}. \emph{{[}Effect size with
95\% confidence interval reported as the primary outcome{]}}. Two-sided
p-values use \emph{α = 0.05}; \emph{{[}multiple-comparison adjustment if
any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Cox Proportional Hazards Regression yielded \emph{{[}estimate{]}} (95\%
CI \emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}).
\emph{{[}Diagnostic / assumption summary{]}}. \emph{{[}Sensitivity
analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/11-survival/cox-proportional-hazards.Rmd\#for-reviewers}{\texttt{cox-proportional-hazards}
--- For Reviewers}

\subsubsection{Cox Regression as
Regression}\label{cox-regression-as-regression}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Cox Regression as Regression} as implemented in \emph{{[}package
vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list
checks performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Cox Regression as Regression yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/07-regression/survival-regression-cox.Rmd\#for-reviewers}{\texttt{survival-regression-cox}
--- For Reviewers}

\subsubsection{Cross-Validation}\label{cross-validation}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Cross-Validation} as implemented in \emph{{[}package vX.Y{]}} in
R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list checks
performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Cross-Validation yielded \emph{{[}estimate{]}} (95\% CI \emph{{[}lower,
upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic / assumption
summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/13-ml/cross-validation.Rmd\#for-reviewers}{\texttt{cross-validation}
--- For Reviewers}

\subsubsection{Crossover RCT Design}\label{crossover-rct-design}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Crossover RCT Design} as implemented in \emph{{[}package
vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list
checks performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Crossover RCT Design yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/14-clinical/rct-design-crossover.Rmd\#for-reviewers}{\texttt{rct-design-crossover}
--- For Reviewers}

\subsubsection{Cutpoint Selection}\label{cutpoint-selection}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Cutpoint Selection} as implemented in \emph{{[}package vX.Y{]}}
in R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list checks
performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Cutpoint Selection yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/14-clinical/cutpoint-selection.Rmd\#for-reviewers}{\texttt{cutpoint-selection}
--- For Reviewers}

\subsubsection{DBSCAN as an ML Tool}\label{dbscan-as-an-ml-tool}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{DBSCAN as an ML Tool} as implemented in \emph{{[}package
vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list
checks performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
DBSCAN as an ML Tool yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/13-ml/clustering-dbscan-ml.Rmd\#for-reviewers}{\texttt{clustering-dbscan-ml}
--- For Reviewers}

\subsubsection{Decision Trees (CART)}\label{decision-trees-cart}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Decision Trees (CART)} as implemented in \emph{{[}package
vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list
checks performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Decision Trees (CART) yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/13-ml/decision-trees-cart.Rmd\#for-reviewers}{\texttt{decision-trees-cart}
--- For Reviewers}

\subsubsection{Diagnostic Test Accuracy}\label{diagnostic-test-accuracy}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Diagnostic Test Accuracy} as implemented in \emph{{[}package
vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list
checks performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Diagnostic Test Accuracy yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/14-clinical/diagnostic-accuracy.Rmd\#for-reviewers}{\texttt{diagnostic-accuracy}
--- For Reviewers}

\subsubsection{Dirichlet Regression}\label{dirichlet-regression}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Dirichlet Regression} as implemented in \emph{{[}package
vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list
checks performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Dirichlet Regression yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/07-regression/dirichlet-regression.Rmd\#for-reviewers}{\texttt{dirichlet-regression}
--- For Reviewers}

\subsubsection{Dropout and Early
Stopping}\label{dropout-and-early-stopping}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Dropout and Early Stopping} as implemented in \emph{{[}package
vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list
checks performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Dropout and Early Stopping yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/13-ml/dropout-early-stopping.Rmd\#for-reviewers}{\texttt{dropout-early-stopping}
--- For Reviewers}

\subsubsection{Dummy Coding}\label{dummy-coding}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Dummy Coding} as implemented in \emph{{[}package vX.Y{]}} in R
\emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list checks
performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Dummy Coding yielded \emph{{[}estimate{]}} (95\% CI \emph{{[}lower,
upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic / assumption
summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/07-regression/dummy-coding.Rmd\#for-reviewers}{\texttt{dummy-coding}
--- For Reviewers}

\subsubsection{Effect (Deviation) Coding}\label{effect-deviation-coding}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Effect (Deviation) Coding} as implemented in \emph{{[}package
vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list
checks performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Effect (Deviation) Coding yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/07-regression/effect-coding.Rmd\#for-reviewers}{\texttt{effect-coding}
--- For Reviewers}

\subsubsection{Elastic Net}\label{elastic-net}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Elastic Net} as implemented in \emph{{[}package vX.Y{]}} in R
\emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list checks
performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Elastic Net yielded \emph{{[}estimate{]}} (95\% CI \emph{{[}lower,
upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic / assumption
summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/07-regression/elastic-net.Rmd\#for-reviewers}{\texttt{elastic-net}
--- For Reviewers}

\subsubsection{Extremely Randomised
Trees}\label{extremely-randomised-trees}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Extremely Randomised Trees} as implemented in \emph{{[}package
vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list
checks performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Extremely Randomised Trees yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/13-ml/extremely-randomized-trees.Rmd\#for-reviewers}{\texttt{extremely-randomized-trees}
--- For Reviewers}

\subsubsection{Factorial Trials}\label{factorial-trials}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Factorial Trials} as implemented in \emph{{[}package vX.Y{]}} in
R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list checks
performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Factorial Trials yielded \emph{{[}estimate{]}} (95\% CI \emph{{[}lower,
upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic / assumption
summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/14-clinical/factorial-trial.Rmd\#for-reviewers}{\texttt{factorial-trial}
--- For Reviewers}

\subsubsection{Feature Engineering}\label{feature-engineering}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Feature Engineering} as implemented in \emph{{[}package vX.Y{]}}
in R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list checks
performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Feature Engineering yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/13-ml/feature-engineering.Rmd\#for-reviewers}{\texttt{feature-engineering}
--- For Reviewers}

\subsubsection{Feature Selection}\label{feature-selection}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Feature Selection} as implemented in \emph{{[}package vX.Y{]}}
in R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list checks
performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Feature Selection yielded \emph{{[}estimate{]}} (95\% CI \emph{{[}lower,
upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic / assumption
summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/13-ml/feature-selection.Rmd\#for-reviewers}{\texttt{feature-selection}
--- For Reviewers}

\subsubsection{Feedforward Networks in
torch}\label{feedforward-networks-in-torch}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Feedforward Networks in torch} as implemented in
\emph{{[}package vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were
checked: \emph{{[}list checks performed{]}}. \emph{{[}Effect size with
95\% confidence interval reported as the primary outcome{]}}. Two-sided
p-values use \emph{α = 0.05}; \emph{{[}multiple-comparison adjustment if
any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Feedforward Networks in torch yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/13-ml/feedforward-networks-torch.Rmd\#for-reviewers}{\texttt{feedforward-networks-torch}
--- For Reviewers}

\subsubsection{Fine-Gray Subdistribution
Hazards}\label{fine-gray-subdistribution-hazards}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Fine-Gray Subdistribution Hazards} as implemented in
\emph{{[}package vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were
checked: \emph{{[}list checks performed{]}}. \emph{{[}Effect size with
95\% confidence interval reported as the primary outcome{]}}. Two-sided
p-values use \emph{α = 0.05}; \emph{{[}multiple-comparison adjustment if
any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Fine-Gray Subdistribution Hazards yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/11-survival/fine-gray-regression.Rmd\#for-reviewers}{\texttt{fine-gray-regression}
--- For Reviewers}

\subsubsection{Frailty Models}\label{frailty-models}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Frailty Models} as implemented in \emph{{[}package vX.Y{]}} in R
\emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list checks
performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Frailty Models yielded \emph{{[}estimate{]}} (95\% CI \emph{{[}lower,
upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic / assumption
summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/11-survival/frailty-models.Rmd\#for-reviewers}{\texttt{frailty-models}
--- For Reviewers}

\subsubsection{GAMMs: Additive Mixed
Models}\label{gamms-additive-mixed-models}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{GAMMs: Additive Mixed Models} as implemented in \emph{{[}package
vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list
checks performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
GAMMs: Additive Mixed Models yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/07-regression/gamm-additive-mixed.Rmd\#for-reviewers}{\texttt{gamm-additive-mixed}
--- For Reviewers}

\subsubsection{GAMs: Introduction}\label{gams-introduction}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{GAMs: Introduction} as implemented in \emph{{[}package vX.Y{]}}
in R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list checks
performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
GAMs: Introduction yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/07-regression/gam-introduction.Rmd\#for-reviewers}{\texttt{gam-introduction}
--- For Reviewers}

\subsubsection{Generalised Estimating
Equations}\label{generalised-estimating-equations}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Generalised Estimating Equations} as implemented in
\emph{{[}package vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were
checked: \emph{{[}list checks performed{]}}. \emph{{[}Effect size with
95\% confidence interval reported as the primary outcome{]}}. Two-sided
p-values use \emph{α = 0.05}; \emph{{[}multiple-comparison adjustment if
any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Generalised Estimating Equations yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/07-regression/generalized-estimating-equations.Rmd\#for-reviewers}{\texttt{generalized-estimating-equations}
--- For Reviewers}

\subsubsection{glmer: Generalised Mixed
Models}\label{glmer-generalised-mixed-models}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{glmer: Generalised Mixed Models} as implemented in
\emph{{[}package vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were
checked: \emph{{[}list checks performed{]}}. \emph{{[}Effect size with
95\% confidence interval reported as the primary outcome{]}}. Two-sided
p-values use \emph{α = 0.05}; \emph{{[}multiple-comparison adjustment if
any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
glmer: Generalised Mixed Models yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/07-regression/glmer-generalized-mixed.Rmd\#for-reviewers}{\texttt{glmer-generalized-mixed}
--- For Reviewers}

\subsubsection{Gradient Boosting}\label{gradient-boosting}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Gradient Boosting} as implemented in \emph{{[}package vX.Y{]}}
in R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list checks
performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Gradient Boosting yielded \emph{{[}estimate{]}} (95\% CI \emph{{[}lower,
upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic / assumption
summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/13-ml/gradient-boosting.Rmd\#for-reviewers}{\texttt{gradient-boosting}
--- For Reviewers}

\subsubsection{Hazard, Survival, and Cumulative
Hazard}\label{hazard-survival-and-cumulative-hazard}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Hazard, Survival, and Cumulative Hazard} as implemented in
\emph{{[}package vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were
checked: \emph{{[}list checks performed{]}}. \emph{{[}Effect size with
95\% confidence interval reported as the primary outcome{]}}. Two-sided
p-values use \emph{α = 0.05}; \emph{{[}multiple-comparison adjustment if
any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Hazard, Survival, and Cumulative Hazard yielded \emph{{[}estimate{]}}
(95\% CI \emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}).
\emph{{[}Diagnostic / assumption summary{]}}. \emph{{[}Sensitivity
analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/11-survival/hazard-survival-cumulative.Rmd\#for-reviewers}{\texttt{hazard-survival-cumulative}
--- For Reviewers}

\subsubsection{Hurdle Models}\label{hurdle-models}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Hurdle Models} as implemented in \emph{{[}package vX.Y{]}} in R
\emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list checks
performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Hurdle Models yielded \emph{{[}estimate{]}} (95\% CI \emph{{[}lower,
upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic / assumption
summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/07-regression/hurdle-models.Rmd\#for-reviewers}{\texttt{hurdle-models}
--- For Reviewers}

\subsubsection{Interactions in
Regression}\label{interactions-in-regression}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Interactions in Regression} as implemented in \emph{{[}package
vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list
checks performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Interactions in Regression yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/07-regression/interactions-in-regression.Rmd\#for-reviewers}{\texttt{interactions-in-regression}
--- For Reviewers}

\subsubsection{Interim Analyses and Group Sequential
Designs}\label{interim-analyses-and-group-sequential-designs}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Interim Analyses and Group Sequential Designs} as implemented in
\emph{{[}package vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were
checked: \emph{{[}list checks performed{]}}. \emph{{[}Effect size with
95\% confidence interval reported as the primary outcome{]}}. Two-sided
p-values use \emph{α = 0.05}; \emph{{[}multiple-comparison adjustment if
any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Interim Analyses and Group Sequential Designs yielded
\emph{{[}estimate{]}} (95\% CI \emph{{[}lower, upper{]}}, p =
\emph{{[}value{]}}). \emph{{[}Diagnostic / assumption summary{]}}.
\emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/14-clinical/interim-analysis-group-sequential.Rmd\#for-reviewers}{\texttt{interim-analysis-group-sequential}
--- For Reviewers}

\subsubsection{Interval-Censored Data}\label{interval-censored-data}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Interval-Censored Data} as implemented in \emph{{[}package
vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list
checks performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Interval-Censored Data yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/11-survival/interval-censored-data.Rmd\#for-reviewers}{\texttt{interval-censored-data}
--- For Reviewers}

\subsubsection{Intraclass Correlation Coefficient
(ICC)}\label{intraclass-correlation-coefficient-icc}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Intraclass Correlation Coefficient (ICC)} as implemented in
\emph{{[}package vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were
checked: \emph{{[}list checks performed{]}}. \emph{{[}Effect size with
95\% confidence interval reported as the primary outcome{]}}. Two-sided
p-values use \emph{α = 0.05}; \emph{{[}multiple-comparison adjustment if
any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Intraclass Correlation Coefficient (ICC) yielded \emph{{[}estimate{]}}
(95\% CI \emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}).
\emph{{[}Diagnostic / assumption summary{]}}. \emph{{[}Sensitivity
analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/14-clinical/icc-continuous-agreement.Rmd\#for-reviewers}{\texttt{icc-continuous-agreement}
--- For Reviewers}

\subsubsection{Isolation Forest}\label{isolation-forest}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Isolation Forest} as implemented in \emph{{[}package vX.Y{]}} in
R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list checks
performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Isolation Forest yielded \emph{{[}estimate{]}} (95\% CI \emph{{[}lower,
upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic / assumption
summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/13-ml/isolation-forest.Rmd\#for-reviewers}{\texttt{isolation-forest}
--- For Reviewers}

\subsubsection{Isotonic Regression
Calibration}\label{isotonic-regression-calibration}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Isotonic Regression Calibration} as implemented in
\emph{{[}package vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were
checked: \emph{{[}list checks performed{]}}. \emph{{[}Effect size with
95\% confidence interval reported as the primary outcome{]}}. Two-sided
p-values use \emph{α = 0.05}; \emph{{[}multiple-comparison adjustment if
any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Isotonic Regression Calibration yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/13-ml/isotonic-regression.Rmd\#for-reviewers}{\texttt{isotonic-regression}
--- For Reviewers}

\subsubsection{ITT vs Per-Protocol
Analysis}\label{itt-vs-per-protocol-analysis}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{ITT vs Per-Protocol Analysis} as implemented in \emph{{[}package
vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list
checks performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
ITT vs Per-Protocol Analysis yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/14-clinical/itt-vs-pp-analysis.Rmd\#for-reviewers}{\texttt{itt-vs-pp-analysis}
--- For Reviewers}

\subsubsection{Joint Longitudinal-Survival
Models}\label{joint-longitudinal-survival-models}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Joint Longitudinal-Survival Models} as implemented in
\emph{{[}package vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were
checked: \emph{{[}list checks performed{]}}. \emph{{[}Effect size with
95\% confidence interval reported as the primary outcome{]}}. Two-sided
p-values use \emph{α = 0.05}; \emph{{[}multiple-comparison adjustment if
any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Joint Longitudinal-Survival Models yielded \emph{{[}estimate{]}} (95\%
CI \emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}).
\emph{{[}Diagnostic / assumption summary{]}}. \emph{{[}Sensitivity
analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/11-survival/joint-longitudinal-survival.Rmd\#for-reviewers}{\texttt{joint-longitudinal-survival}
--- For Reviewers}

\subsubsection{k-Means as an ML Tool}\label{k-means-as-an-ml-tool}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{k-Means as an ML Tool} as implemented in \emph{{[}package
vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list
checks performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
k-Means as an ML Tool yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/13-ml/clustering-kmeans-ml.Rmd\#for-reviewers}{\texttt{clustering-kmeans-ml}
--- For Reviewers}

\subsubsection{k-Nearest Neighbours
Classification}\label{k-nearest-neighbours-classification}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{k-Nearest Neighbours Classification} as implemented in
\emph{{[}package vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were
checked: \emph{{[}list checks performed{]}}. \emph{{[}Effect size with
95\% confidence interval reported as the primary outcome{]}}. Two-sided
p-values use \emph{α = 0.05}; \emph{{[}multiple-comparison adjustment if
any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
k-Nearest Neighbours Classification yielded \emph{{[}estimate{]}} (95\%
CI \emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}).
\emph{{[}Diagnostic / assumption summary{]}}. \emph{{[}Sensitivity
analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/13-ml/knn-classification.Rmd\#for-reviewers}{\texttt{knn-classification}
--- For Reviewers}

\subsubsection{k-Nearest Neighbours
Regression}\label{k-nearest-neighbours-regression}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{k-Nearest Neighbours Regression} as implemented in
\emph{{[}package vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were
checked: \emph{{[}list checks performed{]}}. \emph{{[}Effect size with
95\% confidence interval reported as the primary outcome{]}}. Two-sided
p-values use \emph{α = 0.05}; \emph{{[}multiple-comparison adjustment if
any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
k-Nearest Neighbours Regression yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/13-ml/knn-regression.Rmd\#for-reviewers}{\texttt{knn-regression}
--- For Reviewers}

\subsubsection{Kaplan-Meier Estimation}\label{kaplan-meier-estimation}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Kaplan-Meier Estimation} as implemented in \emph{{[}package
vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list
checks performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Kaplan-Meier Estimation yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/11-survival/kaplan-meier.Rmd\#for-reviewers}{\texttt{kaplan-meier}
--- For Reviewers}

\subsubsection{Kernel SVMs}\label{kernel-svms}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Kernel SVMs} as implemented in \emph{{[}package vX.Y{]}} in R
\emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list checks
performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Kernel SVMs yielded \emph{{[}estimate{]}} (95\% CI \emph{{[}lower,
upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic / assumption
summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/13-ml/svm-kernel.Rmd\#for-reviewers}{\texttt{svm-kernel}
--- For Reviewers}

\subsubsection{Landmark Analysis}\label{landmark-analysis}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Landmark Analysis} as implemented in \emph{{[}package vX.Y{]}}
in R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list checks
performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Landmark Analysis yielded \emph{{[}estimate{]}} (95\% CI \emph{{[}lower,
upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic / assumption
summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/11-survival/landmark-analysis.Rmd\#for-reviewers}{\texttt{landmark-analysis}
--- For Reviewers}

\subsubsection{Lasso Regression}\label{lasso-regression}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Lasso Regression} as implemented in \emph{{[}package vX.Y{]}} in
R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list checks
performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Lasso Regression yielded \emph{{[}estimate{]}} (95\% CI \emph{{[}lower,
upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic / assumption
summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/07-regression/lasso-regression.Rmd\#for-reviewers}{\texttt{lasso-regression}
--- For Reviewers}

\subsubsection{Left Truncation}\label{left-truncation}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Left Truncation} as implemented in \emph{{[}package vX.Y{]}} in
R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list checks
performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Left Truncation yielded \emph{{[}estimate{]}} (95\% CI \emph{{[}lower,
upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic / assumption
summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/11-survival/left-truncation.Rmd\#for-reviewers}{\texttt{left-truncation}
--- For Reviewers}

\subsubsection{Leverage and Influence}\label{leverage-and-influence}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Leverage and Influence} as implemented in \emph{{[}package
vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list
checks performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Leverage and Influence yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/07-regression/leverage-influence.Rmd\#for-reviewers}{\texttt{leverage-influence}
--- For Reviewers}

\subsubsection{LightGBM}\label{lightgbm}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{LightGBM} as implemented in \emph{{[}package vX.Y{]}} in R
\emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list checks
performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
LightGBM yielded \emph{{[}estimate{]}} (95\% CI \emph{{[}lower,
upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic / assumption
summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/13-ml/lightgbm.Rmd\#for-reviewers}{\texttt{lightgbm}
--- For Reviewers}

\subsubsection{Likelihood Ratios}\label{likelihood-ratios}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Likelihood Ratios} as implemented in \emph{{[}package vX.Y{]}}
in R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list checks
performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Likelihood Ratios yielded \emph{{[}estimate{]}} (95\% CI \emph{{[}lower,
upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic / assumption
summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/14-clinical/likelihood-ratios.Rmd\#for-reviewers}{\texttt{likelihood-ratios}
--- For Reviewers}

\subsubsection{LIME Explanations}\label{lime-explanations}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{LIME Explanations} as implemented in \emph{{[}package vX.Y{]}}
in R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list checks
performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
LIME Explanations yielded \emph{{[}estimate{]}} (95\% CI \emph{{[}lower,
upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic / assumption
summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/13-ml/lime-explanations.Rmd\#for-reviewers}{\texttt{lime-explanations}
--- For Reviewers}

\subsubsection{Linear Discriminant as
ML}\label{linear-discriminant-as-ml}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Linear Discriminant as ML} as implemented in \emph{{[}package
vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list
checks performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Linear Discriminant as ML yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/13-ml/linear-discriminant-ml.Rmd\#for-reviewers}{\texttt{linear-discriminant-ml}
--- For Reviewers}

\subsubsection{Linear Regression
Assumptions}\label{linear-regression-assumptions}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Linear Regression Assumptions} as implemented in
\emph{{[}package vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were
checked: \emph{{[}list checks performed{]}}. \emph{{[}Effect size with
95\% confidence interval reported as the primary outcome{]}}. Two-sided
p-values use \emph{α = 0.05}; \emph{{[}multiple-comparison adjustment if
any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Linear Regression Assumptions yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/07-regression/regression-assumptions.Rmd\#for-reviewers}{\texttt{regression-assumptions}
--- For Reviewers}

\subsubsection{Linear Support Vector
Machines}\label{linear-support-vector-machines}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Linear Support Vector Machines} as implemented in
\emph{{[}package vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were
checked: \emph{{[}list checks performed{]}}. \emph{{[}Effect size with
95\% confidence interval reported as the primary outcome{]}}. Two-sided
p-values use \emph{α = 0.05}; \emph{{[}multiple-comparison adjustment if
any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Linear Support Vector Machines yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/13-ml/svm-linear.Rmd\#for-reviewers}{\texttt{svm-linear}
--- For Reviewers}

\subsubsection{Log-Rank Test: Details}\label{log-rank-test-details}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Log-Rank Test: Details} as implemented in \emph{{[}package
vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list
checks performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Log-Rank Test: Details yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/11-survival/log-rank-test-details.Rmd\#for-reviewers}{\texttt{log-rank-test-details}
--- For Reviewers}

\subsubsection{Logistic Regression}\label{logistic-regression}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Logistic Regression} as implemented in \emph{{[}package vX.Y{]}}
in R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list checks
performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Logistic Regression yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/07-regression/logistic-regression.Rmd\#for-reviewers}{\texttt{logistic-regression}
--- For Reviewers}

\subsubsection{Logistic Regression as
ML}\label{logistic-regression-as-ml}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Logistic Regression as ML} as implemented in \emph{{[}package
vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list
checks performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Logistic Regression as ML yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/13-ml/logistic-regression-ml.Rmd\#for-reviewers}{\texttt{logistic-regression-ml}
--- For Reviewers}

\subsubsection{Logistic Regression
Diagnostics}\label{logistic-regression-diagnostics}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Logistic Regression Diagnostics} as implemented in
\emph{{[}package vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were
checked: \emph{{[}list checks performed{]}}. \emph{{[}Effect size with
95\% confidence interval reported as the primary outcome{]}}. Two-sided
p-values use \emph{α = 0.05}; \emph{{[}multiple-comparison adjustment if
any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Logistic Regression Diagnostics yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/07-regression/logistic-regression-diagnostics.Rmd\#for-reviewers}{\texttt{logistic-regression-diagnostics}
--- For Reviewers}

\subsubsection{Minimisation Algorithm}\label{minimisation-algorithm}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Minimisation Algorithm} as implemented in \emph{{[}package
vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list
checks performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Minimisation Algorithm yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/14-clinical/minimization-algorithm.Rmd\#for-reviewers}{\texttt{minimization-algorithm}
--- For Reviewers}

\subsubsection{Missing Data in RCTs}\label{missing-data-in-rcts}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Missing Data in RCTs} as implemented in \emph{{[}package
vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list
checks performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Missing Data in RCTs yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/14-clinical/missing-data-rct.Rmd\#for-reviewers}{\texttt{missing-data-rct}
--- For Reviewers}

\subsubsection{Mixed-Effects Models:
Introduction}\label{mixed-effects-models-introduction}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Mixed-Effects Models: Introduction} as implemented in
\emph{{[}package vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were
checked: \emph{{[}list checks performed{]}}. \emph{{[}Effect size with
95\% confidence interval reported as the primary outcome{]}}. Two-sided
p-values use \emph{α = 0.05}; \emph{{[}multiple-comparison adjustment if
any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Mixed-Effects Models: Introduction yielded \emph{{[}estimate{]}} (95\%
CI \emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}).
\emph{{[}Diagnostic / assumption summary{]}}. \emph{{[}Sensitivity
analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/07-regression/mixed-models-intro.Rmd\#for-reviewers}{\texttt{mixed-models-intro}
--- For Reviewers}

\subsubsection{Model Selection with AIC and
BIC}\label{model-selection-with-aic-and-bic}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Model Selection with AIC and BIC} as implemented in
\emph{{[}package vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were
checked: \emph{{[}list checks performed{]}}. \emph{{[}Effect size with
95\% confidence interval reported as the primary outcome{]}}. Two-sided
p-values use \emph{α = 0.05}; \emph{{[}multiple-comparison adjustment if
any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Model Selection with AIC and BIC yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/07-regression/model-selection-aic-bic.Rmd\#for-reviewers}{\texttt{model-selection-aic-bic}
--- For Reviewers}

\subsubsection{Multi-State Models}\label{multi-state-models}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Multi-State Models} as implemented in \emph{{[}package vX.Y{]}}
in R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list checks
performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Multi-State Models yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/11-survival/multi-state-models.Rmd\#for-reviewers}{\texttt{multi-state-models}
--- For Reviewers}

\subsubsection{Multicollinearity and
VIF}\label{multicollinearity-and-vif}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Multicollinearity and VIF} as implemented in \emph{{[}package
vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list
checks performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Multicollinearity and VIF yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/07-regression/multicollinearity-vif.Rmd\#for-reviewers}{\texttt{multicollinearity-vif}
--- For Reviewers}

\subsubsection{Multinomial Logistic
Regression}\label{multinomial-logistic-regression}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Multinomial Logistic Regression} as implemented in
\emph{{[}package vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were
checked: \emph{{[}list checks performed{]}}. \emph{{[}Effect size with
95\% confidence interval reported as the primary outcome{]}}. Two-sided
p-values use \emph{α = 0.05}; \emph{{[}multiple-comparison adjustment if
any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Multinomial Logistic Regression yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/07-regression/multinomial-logistic.Rmd\#for-reviewers}{\texttt{multinomial-logistic}
--- For Reviewers}

\subsubsection{Multiple Imputation}\label{multiple-imputation}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Multiple Imputation} as implemented in \emph{{[}package vX.Y{]}}
in R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list checks
performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Multiple Imputation yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/14-clinical/multiple-imputation.Rmd\#for-reviewers}{\texttt{multiple-imputation}
--- For Reviewers}

\subsubsection{Multiple Linear
Regression}\label{multiple-linear-regression}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Multiple Linear Regression} as implemented in \emph{{[}package
vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list
checks performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Multiple Linear Regression yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/07-regression/multiple-linear-regression.Rmd\#for-reviewers}{\texttt{multiple-linear-regression}
--- For Reviewers}

\subsubsection{Naive Bayes}\label{naive-bayes}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Naive Bayes} as implemented in \emph{{[}package vX.Y{]}} in R
\emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list checks
performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Naive Bayes yielded \emph{{[}estimate{]}} (95\% CI \emph{{[}lower,
upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic / assumption
summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/13-ml/naive-bayes.Rmd\#for-reviewers}{\texttt{naive-bayes}
--- For Reviewers}

\subsubsection{Negative Binomial
Regression}\label{negative-binomial-regression}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Negative Binomial Regression} as implemented in \emph{{[}package
vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list
checks performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Negative Binomial Regression yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/07-regression/negative-binomial-regression.Rmd\#for-reviewers}{\texttt{negative-binomial-regression}
--- For Reviewers}

\subsubsection{Nested Cross-Validation}\label{nested-cross-validation}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Nested Cross-Validation} as implemented in \emph{{[}package
vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list
checks performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Nested Cross-Validation yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/13-ml/nested-cv.Rmd\#for-reviewers}{\texttt{nested-cv}
--- For Reviewers}

\subsubsection{Nested vs Crossed Random
Effects}\label{nested-vs-crossed-random-effects}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Nested vs Crossed Random Effects} as implemented in
\emph{{[}package vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were
checked: \emph{{[}list checks performed{]}}. \emph{{[}Effect size with
95\% confidence interval reported as the primary outcome{]}}. Two-sided
p-values use \emph{α = 0.05}; \emph{{[}multiple-comparison adjustment if
any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Nested vs Crossed Random Effects yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/07-regression/nested-crossed-random-effects.Rmd\#for-reviewers}{\texttt{nested-crossed-random-effects}
--- For Reviewers}

\subsubsection{Neural Networks:
Introduction}\label{neural-networks-introduction}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Neural Networks: Introduction} as implemented in
\emph{{[}package vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were
checked: \emph{{[}list checks performed{]}}. \emph{{[}Effect size with
95\% confidence interval reported as the primary outcome{]}}. Two-sided
p-values use \emph{α = 0.05}; \emph{{[}multiple-comparison adjustment if
any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Neural Networks: Introduction yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/13-ml/neural-networks-intro.Rmd\#for-reviewers}{\texttt{neural-networks-intro}
--- For Reviewers}

\subsubsection{Non-Inferiority Margin
Selection}\label{non-inferiority-margin-selection}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Non-Inferiority Margin Selection} as implemented in
\emph{{[}package vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were
checked: \emph{{[}list checks performed{]}}. \emph{{[}Effect size with
95\% confidence interval reported as the primary outcome{]}}. Two-sided
p-values use \emph{α = 0.05}; \emph{{[}multiple-comparison adjustment if
any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Non-Inferiority Margin Selection yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/14-clinical/non-inferiority-margin.Rmd\#for-reviewers}{\texttt{non-inferiority-margin}
--- For Reviewers}

\subsubsection{O'Brien-Fleming Boundary}\label{obrien-fleming-boundary}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{O'Brien-Fleming Boundary} as implemented in \emph{{[}package
vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list
checks performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
O'Brien-Fleming Boundary yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/14-clinical/obrien-fleming-boundary.Rmd\#for-reviewers}{\texttt{obrien-fleming-boundary}
--- For Reviewers}

\subsubsection{Offsets in Regression}\label{offsets-in-regression}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Offsets in Regression} as implemented in \emph{{[}package
vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list
checks performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Offsets in Regression yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/07-regression/offsets-in-regression.Rmd\#for-reviewers}{\texttt{offsets-in-regression}
--- For Reviewers}

\subsubsection{Ordinal Logistic
Regression}\label{ordinal-logistic-regression}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Ordinal Logistic Regression} as implemented in \emph{{[}package
vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list
checks performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Ordinal Logistic Regression yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/07-regression/ordinal-logistic.Rmd\#for-reviewers}{\texttt{ordinal-logistic}
--- For Reviewers}

\subsubsection{Parallel-Group RCT
Design}\label{parallel-group-rct-design}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Parallel-Group RCT Design} as implemented in \emph{{[}package
vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list
checks performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Parallel-Group RCT Design yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/14-clinical/rct-design-parallel.Rmd\#for-reviewers}{\texttt{rct-design-parallel}
--- For Reviewers}

\subsubsection{Parametric Survival:
Log-Normal}\label{parametric-survival-log-normal}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Parametric Survival: Log-Normal} as implemented in
\emph{{[}package vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were
checked: \emph{{[}list checks performed{]}}. \emph{{[}Effect size with
95\% confidence interval reported as the primary outcome{]}}. Two-sided
p-values use \emph{α = 0.05}; \emph{{[}multiple-comparison adjustment if
any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Parametric Survival: Log-Normal yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/11-survival/parametric-survival-lognormal.Rmd\#for-reviewers}{\texttt{parametric-survival-lognormal}
--- For Reviewers}

\subsubsection{Parametric Survival:
Weibull}\label{parametric-survival-weibull}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Parametric Survival: Weibull} as implemented in \emph{{[}package
vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list
checks performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Parametric Survival: Weibull yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/11-survival/parametric-survival-weibull.Rmd\#for-reviewers}{\texttt{parametric-survival-weibull}
--- For Reviewers}

\subsubsection{Partial Correlation}\label{partial-correlation}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Partial Correlation} as implemented in \emph{{[}package vX.Y{]}}
in R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list checks
performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Partial Correlation yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/07-regression/partial-correlation.Rmd\#for-reviewers}{\texttt{partial-correlation}
--- For Reviewers}

\subsubsection{Partial Dependence Plots}\label{partial-dependence-plots}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Partial Dependence Plots} as implemented in \emph{{[}package
vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list
checks performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Partial Dependence Plots yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/13-ml/partial-dependence-plots.Rmd\#for-reviewers}{\texttt{partial-dependence-plots}
--- For Reviewers}

\subsubsection{Platt Scaling}\label{platt-scaling}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Platt Scaling} as implemented in \emph{{[}package vX.Y{]}} in R
\emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list checks
performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Platt Scaling yielded \emph{{[}estimate{]}} (95\% CI \emph{{[}lower,
upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic / assumption
summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/13-ml/platt-scaling.Rmd\#for-reviewers}{\texttt{platt-scaling}
--- For Reviewers}

\subsubsection{Pocock Boundary}\label{pocock-boundary}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Pocock Boundary} as implemented in \emph{{[}package vX.Y{]}} in
R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list checks
performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Pocock Boundary yielded \emph{{[}estimate{]}} (95\% CI \emph{{[}lower,
upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic / assumption
summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/14-clinical/pocock-boundary.Rmd\#for-reviewers}{\texttt{pocock-boundary}
--- For Reviewers}

\subsubsection{Poisson Regression}\label{poisson-regression}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Poisson Regression} as implemented in \emph{{[}package vX.Y{]}}
in R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list checks
performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Poisson Regression yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/07-regression/poisson-regression.Rmd\#for-reviewers}{\texttt{poisson-regression}
--- For Reviewers}

\subsubsection{Polynomial Regression}\label{polynomial-regression}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Polynomial Regression} as implemented in \emph{{[}package
vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list
checks performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Polynomial Regression yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/07-regression/polynomial-regression.Rmd\#for-reviewers}{\texttt{polynomial-regression}
--- For Reviewers}

\subsubsection{Predictive Values and
Prevalence}\label{predictive-values-and-prevalence}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Predictive Values and Prevalence} as implemented in
\emph{{[}package vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were
checked: \emph{{[}list checks performed{]}}. \emph{{[}Effect size with
95\% confidence interval reported as the primary outcome{]}}. Two-sided
p-values use \emph{α = 0.05}; \emph{{[}multiple-comparison adjustment if
any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Predictive Values and Prevalence yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/14-clinical/predictive-values-prevalence.Rmd\#for-reviewers}{\texttt{predictive-values-prevalence}
--- For Reviewers}

\subsubsection{Preprocessing with
recipes}\label{preprocessing-with-recipes}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Preprocessing with recipes} as implemented in \emph{{[}package
vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list
checks performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Preprocessing with recipes yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/13-ml/recipes-preprocessing.Rmd\#for-reviewers}{\texttt{recipes-preprocessing}
--- For Reviewers}

\subsubsection{Probit Regression}\label{probit-regression}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Probit Regression} as implemented in \emph{{[}package vX.Y{]}}
in R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list checks
performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Probit Regression yielded \emph{{[}estimate{]}} (95\% CI \emph{{[}lower,
upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic / assumption
summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/07-regression/probit-regression.Rmd\#for-reviewers}{\texttt{probit-regression}
--- For Reviewers}

\subsubsection{Quantile Regression}\label{quantile-regression}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Quantile Regression} as implemented in \emph{{[}package vX.Y{]}}
in R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list checks
performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Quantile Regression yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/07-regression/quantile-regression.Rmd\#for-reviewers}{\texttt{quantile-regression}
--- For Reviewers}

\subsubsection{Quasi-Poisson Regression}\label{quasi-poisson-regression}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Quasi-Poisson Regression} as implemented in \emph{{[}package
vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list
checks performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Quasi-Poisson Regression yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/07-regression/quasi-poisson.Rmd\#for-reviewers}{\texttt{quasi-poisson}
--- For Reviewers}

\subsubsection{R-Squared and Adjusted
R-Squared}\label{r-squared-and-adjusted-r-squared}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{R-Squared and Adjusted R-Squared} as implemented in
\emph{{[}package vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were
checked: \emph{{[}list checks performed{]}}. \emph{{[}Effect size with
95\% confidence interval reported as the primary outcome{]}}. Two-sided
p-values use \emph{α = 0.05}; \emph{{[}multiple-comparison adjustment if
any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
R-Squared and Adjusted R-Squared yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/07-regression/r-squared-adjusted.Rmd\#for-reviewers}{\texttt{r-squared-adjusted}
--- For Reviewers}

\subsubsection{Random Forests}\label{random-forests}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Random Forests} as implemented in \emph{{[}package vX.Y{]}} in R
\emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list checks
performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Random Forests yielded \emph{{[}estimate{]}} (95\% CI \emph{{[}lower,
upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic / assumption
summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/13-ml/random-forest.Rmd\#for-reviewers}{\texttt{random-forest}
--- For Reviewers}

\subsubsection{Random-Intercept Models}\label{random-intercept-models}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Random-Intercept Models} as implemented in \emph{{[}package
vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list
checks performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Random-Intercept Models yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/07-regression/random-intercepts.Rmd\#for-reviewers}{\texttt{random-intercepts}
--- For Reviewers}

\subsubsection{Random-Slope Models}\label{random-slope-models}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Random-Slope Models} as implemented in \emph{{[}package vX.Y{]}}
in R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list checks
performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Random-Slope Models yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/07-regression/random-slopes.Rmd\#for-reviewers}{\texttt{random-slopes}
--- For Reviewers}

\subsubsection{Randomisation Methods}\label{randomisation-methods}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Randomisation Methods} as implemented in \emph{{[}package
vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list
checks performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Randomisation Methods yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/14-clinical/randomization-methods.Rmd\#for-reviewers}{\texttt{randomization-methods}
--- For Reviewers}

\subsubsection{Recurrent Events}\label{recurrent-events}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Recurrent Events} as implemented in \emph{{[}package vX.Y{]}} in
R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list checks
performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Recurrent Events yielded \emph{{[}estimate{]}} (95\% CI \emph{{[}lower,
upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic / assumption
summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/11-survival/recurrent-events.Rmd\#for-reviewers}{\texttt{recurrent-events}
--- For Reviewers}

\subsubsection{Regularisation in ML}\label{regularisation-in-ml}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Regularisation in ML} as implemented in \emph{{[}package
vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list
checks performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Regularisation in ML yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/13-ml/regularization-ridge-lasso-ml.Rmd\#for-reviewers}{\texttt{regularization-ridge-lasso-ml}
--- For Reviewers}

\subsubsection{Reliability and Cronbach's
Alpha}\label{reliability-and-cronbachs-alpha}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Reliability and Cronbach's Alpha} as implemented in
\emph{{[}package vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were
checked: \emph{{[}list checks performed{]}}. \emph{{[}Effect size with
95\% confidence interval reported as the primary outcome{]}}. Two-sided
p-values use \emph{α = 0.05}; \emph{{[}multiple-comparison adjustment if
any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Reliability and Cronbach's Alpha yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/14-clinical/reliability-cronbach-alpha.Rmd\#for-reviewers}{\texttt{reliability-cronbach-alpha}
--- For Reviewers}

\subsubsection{Residual Diagnostics}\label{residual-diagnostics}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Residual Diagnostics} as implemented in \emph{{[}package
vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list
checks performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Residual Diagnostics yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/07-regression/residual-diagnostics.Rmd\#for-reviewers}{\texttt{residual-diagnostics}
--- For Reviewers}

\subsubsection{Restricted Mean Survival Time
(RMST)}\label{restricted-mean-survival-time-rmst}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Restricted Mean Survival Time (RMST)} as implemented in
\emph{{[}package vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were
checked: \emph{{[}list checks performed{]}}. \emph{{[}Effect size with
95\% confidence interval reported as the primary outcome{]}}. Two-sided
p-values use \emph{α = 0.05}; \emph{{[}multiple-comparison adjustment if
any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Restricted Mean Survival Time (RMST) yielded \emph{{[}estimate{]}} (95\%
CI \emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}).
\emph{{[}Diagnostic / assumption summary{]}}. \emph{{[}Sensitivity
analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/11-survival/restricted-mean-survival-time.Rmd\#for-reviewers}{\texttt{restricted-mean-survival-time}
--- For Reviewers}

\subsubsection{Ridge Regression}\label{ridge-regression}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Ridge Regression} as implemented in \emph{{[}package vX.Y{]}} in
R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list checks
performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Ridge Regression yielded \emph{{[}estimate{]}} (95\% CI \emph{{[}lower,
upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic / assumption
summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/07-regression/ridge-regression.Rmd\#for-reviewers}{\texttt{ridge-regression}
--- For Reviewers}

\subsubsection{RNNs and LSTMs:
Introduction}\label{rnns-and-lstms-introduction}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{RNNs and LSTMs: Introduction} as implemented in \emph{{[}package
vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list
checks performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
RNNs and LSTMs: Introduction yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/13-ml/rnn-lstm-intro.Rmd\#for-reviewers}{\texttt{rnn-lstm-intro}
--- For Reviewers}

\subsubsection{Robust Regression}\label{robust-regression}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Robust Regression} as implemented in \emph{{[}package vX.Y{]}}
in R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list checks
performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Robust Regression yielded \emph{{[}estimate{]}} (95\% CI \emph{{[}lower,
upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic / assumption
summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/07-regression/robust-regression.Rmd\#for-reviewers}{\texttt{robust-regression}
--- For Reviewers}

\subsubsection{ROC Analysis}\label{roc-analysis}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{ROC Analysis} as implemented in \emph{{[}package vX.Y{]}} in R
\emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list checks
performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
ROC Analysis yielded \emph{{[}estimate{]}} (95\% CI \emph{{[}lower,
upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic / assumption
summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/14-clinical/roc-analysis.Rmd\#for-reviewers}{\texttt{roc-analysis}
--- For Reviewers}

\subsubsection{ROC and AUC for Logistic
Models}\label{roc-and-auc-for-logistic-models}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{ROC and AUC for Logistic Models} as implemented in
\emph{{[}package vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were
checked: \emph{{[}list checks performed{]}}. \emph{{[}Effect size with
95\% confidence interval reported as the primary outcome{]}}. Two-sided
p-values use \emph{α = 0.05}; \emph{{[}multiple-comparison adjustment if
any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
ROC and AUC for Logistic Models yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/07-regression/roc-auc-for-logistic.Rmd\#for-reviewers}{\texttt{roc-auc-for-logistic}
--- For Reviewers}

\subsubsection{Royston-Parmar Flexible Parametric
Models}\label{royston-parmar-flexible-parametric-models}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Royston-Parmar Flexible Parametric Models} as implemented in
\emph{{[}package vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were
checked: \emph{{[}list checks performed{]}}. \emph{{[}Effect size with
95\% confidence interval reported as the primary outcome{]}}. Two-sided
p-values use \emph{α = 0.05}; \emph{{[}multiple-comparison adjustment if
any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Royston-Parmar Flexible Parametric Models yielded \emph{{[}estimate{]}}
(95\% CI \emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}).
\emph{{[}Diagnostic / assumption summary{]}}. \emph{{[}Sensitivity
analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/11-survival/flexible-parametric-royston-parmar.Rmd\#for-reviewers}{\texttt{flexible-parametric-royston-parmar}
--- For Reviewers}

\subsubsection{Sample Size
Re-Estimation}\label{sample-size-re-estimation}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Sample Size Re-Estimation} as implemented in \emph{{[}package
vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list
checks performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Sample Size Re-Estimation yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/14-clinical/sample-size-reestimation.Rmd\#for-reviewers}{\texttt{sample-size-reestimation}
--- For Reviewers}

\subsubsection{Schoenfeld Residuals}\label{schoenfeld-residuals}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Schoenfeld Residuals} as implemented in \emph{{[}package
vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list
checks performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Schoenfeld Residuals yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/11-survival/schoenfeld-residuals.Rmd\#for-reviewers}{\texttt{schoenfeld-residuals}
--- For Reviewers}

\subsubsection{Sensitivity Analyses in Clinical
Trials}\label{sensitivity-analyses-in-clinical-trials}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Sensitivity Analyses in Clinical Trials} as implemented in
\emph{{[}package vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were
checked: \emph{{[}list checks performed{]}}. \emph{{[}Effect size with
95\% confidence interval reported as the primary outcome{]}}. Two-sided
p-values use \emph{α = 0.05}; \emph{{[}multiple-comparison adjustment if
any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Sensitivity Analyses in Clinical Trials yielded \emph{{[}estimate{]}}
(95\% CI \emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}).
\emph{{[}Diagnostic / assumption summary{]}}. \emph{{[}Sensitivity
analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/14-clinical/sensitivity-analysis-clinical.Rmd\#for-reviewers}{\texttt{sensitivity-analysis-clinical}
--- For Reviewers}

\subsubsection{SHAP Values}\label{shap-values}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{SHAP Values} as implemented in \emph{{[}package vX.Y{]}} in R
\emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list checks
performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
SHAP Values yielded \emph{{[}estimate{]}} (95\% CI \emph{{[}lower,
upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic / assumption
summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/13-ml/shap-values.Rmd\#for-reviewers}{\texttt{shap-values}
--- For Reviewers}

\subsubsection{Simple Linear Regression}\label{simple-linear-regression}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Simple Linear Regression} as implemented in \emph{{[}package
vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list
checks performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Simple Linear Regression yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/07-regression/simple-linear-regression.Rmd\#for-reviewers}{\texttt{simple-linear-regression}
--- For Reviewers}

\subsubsection{Simulating Survival Data}\label{simulating-survival-data}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Simulating Survival Data} as implemented in \emph{{[}package
vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list
checks performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Simulating Survival Data yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/11-survival/survival-simulation.Rmd\#for-reviewers}{\texttt{survival-simulation}
--- For Reviewers}

\subsubsection{SMOTE for Imbalanced
Classes}\label{smote-for-imbalanced-classes}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{SMOTE for Imbalanced Classes} as implemented in \emph{{[}package
vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list
checks performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
SMOTE for Imbalanced Classes yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/13-ml/smote-imbalanced.Rmd\#for-reviewers}{\texttt{smote-imbalanced}
--- For Reviewers}

\subsubsection{Spline Regression}\label{spline-regression}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Spline Regression} as implemented in \emph{{[}package vX.Y{]}}
in R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list checks
performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Spline Regression yielded \emph{{[}estimate{]}} (95\% CI \emph{{[}lower,
upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic / assumption
summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/07-regression/spline-regression.Rmd\#for-reviewers}{\texttt{spline-regression}
--- For Reviewers}

\subsubsection{Stacking Ensembles}\label{stacking-ensembles}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Stacking Ensembles} as implemented in \emph{{[}package vX.Y{]}}
in R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list checks
performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Stacking Ensembles yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/13-ml/stacking-ensembles.Rmd\#for-reviewers}{\texttt{stacking-ensembles}
--- For Reviewers}

\subsubsection{Stepped-Wedge Trial}\label{stepped-wedge-trial}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Stepped-Wedge Trial} as implemented in \emph{{[}package vX.Y{]}}
in R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list checks
performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Stepped-Wedge Trial yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/14-clinical/stepped-wedge-trial.Rmd\#for-reviewers}{\texttt{stepped-wedge-trial}
--- For Reviewers}

\subsubsection{Stepwise Regression:
Pitfalls}\label{stepwise-regression-pitfalls}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Stepwise Regression: Pitfalls} as implemented in
\emph{{[}package vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were
checked: \emph{{[}list checks performed{]}}. \emph{{[}Effect size with
95\% confidence interval reported as the primary outcome{]}}. Two-sided
p-values use \emph{α = 0.05}; \emph{{[}multiple-comparison adjustment if
any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Stepwise Regression: Pitfalls yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/07-regression/stepwise-regression-pitfalls.Rmd\#for-reviewers}{\texttt{stepwise-regression-pitfalls}
--- For Reviewers}

\subsubsection{Stratified Cox Models}\label{stratified-cox-models}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Stratified Cox Models} as implemented in \emph{{[}package
vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list
checks performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Stratified Cox Models yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/11-survival/stratified-cox.Rmd\#for-reviewers}{\texttt{stratified-cox}
--- For Reviewers}

\subsubsection{Stratified Randomisation}\label{stratified-randomisation}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Stratified Randomisation} as implemented in \emph{{[}package
vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list
checks performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Stratified Randomisation yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/14-clinical/stratified-randomization.Rmd\#for-reviewers}{\texttt{stratified-randomization}
--- For Reviewers}

\subsubsection{Subgroup Analyses}\label{subgroup-analyses}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Subgroup Analyses} as implemented in \emph{{[}package vX.Y{]}}
in R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list checks
performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Subgroup Analyses yielded \emph{{[}estimate{]}} (95\% CI \emph{{[}lower,
upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic / assumption
summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/14-clinical/subgroup-analysis.Rmd\#for-reviewers}{\texttt{subgroup-analysis}
--- For Reviewers}

\subsubsection{Subgroup Forest Plots}\label{subgroup-forest-plots}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Subgroup Forest Plots} as implemented in \emph{{[}package
vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list
checks performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Subgroup Forest Plots yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/14-clinical/forest-plot-subgroup.Rmd\#for-reviewers}{\texttt{forest-plot-subgroup}
--- For Reviewers}

\subsubsection{Supervised Learning:
Overview}\label{supervised-learning-overview}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Supervised Learning: Overview} as implemented in
\emph{{[}package vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were
checked: \emph{{[}list checks performed{]}}. \emph{{[}Effect size with
95\% confidence interval reported as the primary outcome{]}}. Two-sided
p-values use \emph{α = 0.05}; \emph{{[}multiple-comparison adjustment if
any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Supervised Learning: Overview yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/13-ml/supervised-learning-overview.Rmd\#for-reviewers}{\texttt{supervised-learning-overview}
--- For Reviewers}

\subsubsection{The Bias-Variance
Tradeoff}\label{the-bias-variance-tradeoff}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{The Bias-Variance Tradeoff} as implemented in \emph{{[}package
vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list
checks performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
The Bias-Variance Tradeoff yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/13-ml/bias-variance-tradeoff.Rmd\#for-reviewers}{\texttt{bias-variance-tradeoff}
--- For Reviewers}

\subsubsection{The Brier Score for
Survival}\label{the-brier-score-for-survival}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{The Brier Score for Survival} as implemented in \emph{{[}package
vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list
checks performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
The Brier Score for Survival yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/11-survival/brier-score-survival.Rmd\#for-reviewers}{\texttt{brier-score-survival}
--- For Reviewers}

\subsubsection{The Nelson-Aalen
Estimator}\label{the-nelson-aalen-estimator}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{The Nelson-Aalen Estimator} as implemented in \emph{{[}package
vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list
checks performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
The Nelson-Aalen Estimator yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/11-survival/nelson-aalen-estimator.Rmd\#for-reviewers}{\texttt{nelson-aalen-estimator}
--- For Reviewers}

\subsubsection{Time-Dependent ROC}\label{time-dependent-roc}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Time-Dependent ROC} as implemented in \emph{{[}package vX.Y{]}}
in R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list checks
performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Time-Dependent ROC yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/11-survival/time-dependent-roc.Rmd\#for-reviewers}{\texttt{time-dependent-roc}
--- For Reviewers}

\subsubsection{Time-Varying Covariates}\label{time-varying-covariates}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Time-Varying Covariates} as implemented in \emph{{[}package
vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list
checks performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Time-Varying Covariates yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/11-survival/time-varying-covariates.Rmd\#for-reviewers}{\texttt{time-varying-covariates}
--- For Reviewers}

\subsubsection{Tobit Regression}\label{tobit-regression}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Tobit Regression} as implemented in \emph{{[}package vX.Y{]}} in
R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list checks
performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Tobit Regression yielded \emph{{[}estimate{]}} (95\% CI \emph{{[}lower,
upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic / assumption
summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/07-regression/tobit-regression.Rmd\#for-reviewers}{\texttt{tobit-regression}
--- For Reviewers}

\subsubsection{Train-Test Splits}\label{train-test-splits}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Train-Test Splits} as implemented in \emph{{[}package vX.Y{]}}
in R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list checks
performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Train-Test Splits yielded \emph{{[}estimate{]}} (95\% CI \emph{{[}lower,
upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic / assumption
summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/13-ml/train-test-split.Rmd\#for-reviewers}{\texttt{train-test-split}
--- For Reviewers}

\subsubsection{Transformers: Overview}\label{transformers-overview}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Transformers: Overview} as implemented in \emph{{[}package
vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list
checks performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Transformers: Overview yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/13-ml/transformer-overview.Rmd\#for-reviewers}{\texttt{transformer-overview}
--- For Reviewers}

\subsubsection{Truncated Regression}\label{truncated-regression}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Truncated Regression} as implemented in \emph{{[}package
vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list
checks performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Truncated Regression yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/07-regression/truncated-regression.Rmd\#for-reviewers}{\texttt{truncated-regression}
--- For Reviewers}

\subsubsection{Variable Importance}\label{variable-importance}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Variable Importance} as implemented in \emph{{[}package vX.Y{]}}
in R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list checks
performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Variable Importance yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/13-ml/variable-importance.Rmd\#for-reviewers}{\texttt{variable-importance}
--- For Reviewers}

\subsubsection{Weighted Kappa}\label{weighted-kappa}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Weighted Kappa} as implemented in \emph{{[}package vX.Y{]}} in R
\emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list checks
performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Weighted Kappa yielded \emph{{[}estimate{]}} (95\% CI \emph{{[}lower,
upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic / assumption
summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/14-clinical/weighted-kappa.Rmd\#for-reviewers}{\texttt{weighted-kappa}
--- For Reviewers}

\subsubsection{Weighted Log-Rank Tests}\label{weighted-log-rank-tests}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Weighted Log-Rank Tests} as implemented in \emph{{[}package
vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list
checks performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Weighted Log-Rank Tests yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/11-survival/weighted-log-rank.Rmd\#for-reviewers}{\texttt{weighted-log-rank}
--- For Reviewers}

\subsubsection{Worked lme4::lmer
Examples}\label{worked-lme4lmer-examples}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Worked lme4::lmer Examples} as implemented in \emph{{[}package
vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list
checks performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Worked lme4::lmer Examples yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/07-regression/lme4-lmer-examples.Rmd\#for-reviewers}{\texttt{lme4-lmer-examples}
--- For Reviewers}

\subsubsection{XGBoost}\label{xgboost}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{XGBoost} as implemented in \emph{{[}package vX.Y{]}} in R
\emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list checks
performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
XGBoost yielded \emph{{[}estimate{]}} (95\% CI \emph{{[}lower,
upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic / assumption
summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/13-ml/xgboost.Rmd\#for-reviewers}{\texttt{xgboost} ---
For Reviewers}

\subsubsection{Zero-Inflated Models}\label{zero-inflated-models}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Zero-Inflated Models} as implemented in \emph{{[}package
vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list
checks performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Zero-Inflated Models yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/07-regression/zero-inflated-models.Rmd\#for-reviewers}{\texttt{zero-inflated-models}
--- For Reviewers}

\end{document}
