% Options for packages loaded elsewhere
\PassOptionsToPackage{unicode}{hyperref}
\PassOptionsToPackage{hyphens}{url}
\documentclass[
]{article}
\usepackage{xcolor}
\usepackage[margin=1in]{geometry}
\usepackage{amsmath,amssymb}
\setcounter{secnumdepth}{-\maxdimen} % remove section numbering
\usepackage{iftex}
\ifPDFTeX
  \usepackage[T1]{fontenc}
  \usepackage[utf8]{inputenc}
  \usepackage{textcomp} % provide euro and other symbols
\else % if luatex or xetex
  \usepackage{unicode-math} % this also loads fontspec
  \defaultfontfeatures{Scale=MatchLowercase}
  \defaultfontfeatures[\rmfamily]{Ligatures=TeX,Scale=1}
\fi
\usepackage{lmodern}
\ifPDFTeX\else
  % xetex/luatex font selection
\fi
% Use upquote if available, for straight quotes in verbatim environments
\IfFileExists{upquote.sty}{\usepackage{upquote}}{}
\IfFileExists{microtype.sty}{% use microtype if available
  \usepackage[]{microtype}
  \UseMicrotypeSet[protrusion]{basicmath} % disable protrusion for tt fonts
}{}
\makeatletter
\@ifundefined{KOMAClassName}{% if non-KOMA class
  \IfFileExists{parskip.sty}{%
    \usepackage{parskip}
  }{% else
    \setlength{\parindent}{0pt}
    \setlength{\parskip}{6pt plus 2pt minus 1pt}}
}{% if KOMA class
  \KOMAoptions{parskip=half}}
\makeatother
\usepackage{graphicx}
\makeatletter
\newsavebox\pandoc@box
\newcommand*\pandocbounded[1]{% scales image to fit in text height/width
  \sbox\pandoc@box{#1}%
  \Gscale@div\@tempa{\textheight}{\dimexpr\ht\pandoc@box+\dp\pandoc@box\relax}%
  \Gscale@div\@tempb{\linewidth}{\wd\pandoc@box}%
  \ifdim\@tempb\p@<\@tempa\p@\let\@tempa\@tempb\fi% select the smaller of both
  \ifdim\@tempa\p@<\p@\scalebox{\@tempa}{\usebox\pandoc@box}%
  \else\usebox{\pandoc@box}%
  \fi%
}
% Set default figure placement to htbp
\def\fps@figure{htbp}
\makeatother
\setlength{\emergencystretch}{3em} % prevent overfull lines
\providecommand{\tightlist}{%
  \setlength{\itemsep}{0pt}\setlength{\parskip}{0pt}}
\usepackage{bookmark}
\IfFileExists{xurl.sty}{\usepackage{xurl}}{} % add URL line breaks if available
\urlstyle{same}
\hypersetup{
  pdftitle={ARRIVE --- animal research},
  hidelinks,
  pdfcreator={LaTeX via pandoc}}

\title{ARRIVE --- animal research}
\author{}
\date{\vspace{-2.5em}}

\begin{document}
\maketitle

\section{ARRIVE --- animal research}\label{arrive-animal-research}

ARRIVE --- \emph{Animal Research: Reporting of In Vivo Experiments} ---
covers preclinical animal research. The 21-item checklist is at
\url{https://arriveguidelines.org/}. The templates below cover the
statistical analyses most commonly required.

This page contains \textbf{50 method-specific templates}. Each is a
skeleton with placeholders in italics; the prose is written so you can
paste it into a manuscript and replace only the bracketed values.

\subsubsection{Alignment with BWA and
Bowtie}\label{alignment-with-bwa-and-bowtie}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Alignment with BWA and Bowtie} as implemented in
\emph{{[}package vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were
checked: \emph{{[}list checks performed{]}}. \emph{{[}Effect size with
95\% confidence interval reported as the primary outcome{]}}. Two-sided
p-values use \emph{α = 0.05}; \emph{{[}multiple-comparison adjustment if
any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Alignment with BWA and Bowtie yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/12-bioinformatics/bwa-bowtie-alignment.Rmd\#for-reviewers}{\texttt{bwa-bowtie-alignment}
--- For Reviewers}

\subsubsection{ATAC-Seq Analysis}\label{atac-seq-analysis}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{ATAC-Seq Analysis} as implemented in \emph{{[}package vX.Y{]}}
in R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list checks
performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
ATAC-Seq Analysis yielded \emph{{[}estimate{]}} (95\% CI \emph{{[}lower,
upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic / assumption
summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/12-bioinformatics/atac-seq-analysis.Rmd\#for-reviewers}{\texttt{atac-seq-analysis}
--- For Reviewers}

\subsubsection{Batch Correction with
ComBat}\label{batch-correction-with-combat}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Batch Correction with ComBat} as implemented in \emph{{[}package
vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list
checks performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Batch Correction with ComBat yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/12-bioinformatics/batch-correction-combat.Rmd\#for-reviewers}{\texttt{batch-correction-combat}
--- For Reviewers}

\subsubsection{Bulk RNA-seq Differential Expression with
DESeq2}\label{bulk-rna-seq-differential-expression-with-deseq2}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Bulk RNA-seq Differential Expression with DESeq2} as implemented
in \emph{{[}package vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were
checked: \emph{{[}list checks performed{]}}. \emph{{[}Effect size with
95\% confidence interval reported as the primary outcome{]}}. Two-sided
p-values use \emph{α = 0.05}; \emph{{[}multiple-comparison adjustment if
any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Bulk RNA-seq Differential Expression with DESeq2 yielded
\emph{{[}estimate{]}} (95\% CI \emph{{[}lower, upper{]}}, p =
\emph{{[}value{]}}). \emph{{[}Diagnostic / assumption summary{]}}.
\emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/12-bioinformatics/bulk-rnaseq-differential-expression.Rmd\#for-reviewers}{\texttt{bulk-rnaseq-differential-expression}
--- For Reviewers}

\subsubsection{Cell-Type Annotation}\label{cell-type-annotation}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Cell-Type Annotation} as implemented in \emph{{[}package
vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list
checks performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Cell-Type Annotation yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/12-bioinformatics/scrna-cell-type-annotation.Rmd\#for-reviewers}{\texttt{scrna-cell-type-annotation}
--- For Reviewers}

\subsubsection{ChIP-Seq Analysis}\label{chip-seq-analysis}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{ChIP-Seq Analysis} as implemented in \emph{{[}package vX.Y{]}}
in R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list checks
performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
ChIP-Seq Analysis yielded \emph{{[}estimate{]}} (95\% CI \emph{{[}lower,
upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic / assumption
summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/12-bioinformatics/chip-seq-analysis.Rmd\#for-reviewers}{\texttt{chip-seq-analysis}
--- For Reviewers}

\subsubsection{Copy Number Variation
Analysis}\label{copy-number-variation-analysis}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Copy Number Variation Analysis} as implemented in
\emph{{[}package vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were
checked: \emph{{[}list checks performed{]}}. \emph{{[}Effect size with
95\% confidence interval reported as the primary outcome{]}}. Two-sided
p-values use \emph{α = 0.05}; \emph{{[}multiple-comparison adjustment if
any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Copy Number Variation Analysis yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/12-bioinformatics/cnv-analysis.Rmd\#for-reviewers}{\texttt{cnv-analysis}
--- For Reviewers}

\subsubsection{Counting Reads with
featureCounts}\label{counting-reads-with-featurecounts}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Counting Reads with featureCounts} as implemented in
\emph{{[}package vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were
checked: \emph{{[}list checks performed{]}}. \emph{{[}Effect size with
95\% confidence interval reported as the primary outcome{]}}. Two-sided
p-values use \emph{α = 0.05}; \emph{{[}multiple-comparison adjustment if
any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Counting Reads with featureCounts yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/12-bioinformatics/counting-reads-feature-counts.Rmd\#for-reviewers}{\texttt{counting-reads-feature-counts}
--- For Reviewers}

\subsubsection{Differential Expression with
edgeR}\label{differential-expression-with-edger}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Differential Expression with edgeR} as implemented in
\emph{{[}package vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were
checked: \emph{{[}list checks performed{]}}. \emph{{[}Effect size with
95\% confidence interval reported as the primary outcome{]}}. Two-sided
p-values use \emph{α = 0.05}; \emph{{[}multiple-comparison adjustment if
any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Differential Expression with edgeR yielded \emph{{[}estimate{]}} (95\%
CI \emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}).
\emph{{[}Diagnostic / assumption summary{]}}. \emph{{[}Sensitivity
analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/12-bioinformatics/edger-differential-expression.Rmd\#for-reviewers}{\texttt{edger-differential-expression}
--- For Reviewers}

\subsubsection{Differential Expression with
limma-voom}\label{differential-expression-with-limma-voom}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Differential Expression with limma-voom} as implemented in
\emph{{[}package vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were
checked: \emph{{[}list checks performed{]}}. \emph{{[}Effect size with
95\% confidence interval reported as the primary outcome{]}}. Two-sided
p-values use \emph{α = 0.05}; \emph{{[}multiple-comparison adjustment if
any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Differential Expression with limma-voom yielded \emph{{[}estimate{]}}
(95\% CI \emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}).
\emph{{[}Diagnostic / assumption summary{]}}. \emph{{[}Sensitivity
analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/12-bioinformatics/limma-voom.Rmd\#for-reviewers}{\texttt{limma-voom}
--- For Reviewers}

\subsubsection{DNA Methylation Analysis}\label{dna-methylation-analysis}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{DNA Methylation Analysis} as implemented in \emph{{[}package
vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list
checks performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
DNA Methylation Analysis yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/12-bioinformatics/methylation-analysis.Rmd\#for-reviewers}{\texttt{methylation-analysis}
--- For Reviewers}

\subsubsection{Drug-Target Interaction
Mining}\label{drug-target-interaction-mining}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Drug-Target Interaction Mining} as implemented in
\emph{{[}package vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were
checked: \emph{{[}list checks performed{]}}. \emph{{[}Effect size with
95\% confidence interval reported as the primary outcome{]}}. Two-sided
p-values use \emph{α = 0.05}; \emph{{[}multiple-comparison adjustment if
any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Drug-Target Interaction Mining yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/12-bioinformatics/drug-target-interaction.Rmd\#for-reviewers}{\texttt{drug-target-interaction}
--- For Reviewers}

\subsubsection{FASTQ Quality Control}\label{fastq-quality-control}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{FASTQ Quality Control} as implemented in \emph{{[}package
vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list
checks performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
FASTQ Quality Control yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/12-bioinformatics/fastq-quality-control.Rmd\#for-reviewers}{\texttt{fastq-quality-control}
--- For Reviewers}

\subsubsection{Finding Marker Genes}\label{finding-marker-genes}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Finding Marker Genes} as implemented in \emph{{[}package
vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list
checks performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Finding Marker Genes yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/12-bioinformatics/scrna-marker-genes.Rmd\#for-reviewers}{\texttt{scrna-marker-genes}
--- For Reviewers}

\subsubsection{Gene Annotation with
biomaRt}\label{gene-annotation-with-biomart}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Gene Annotation with biomaRt} as implemented in \emph{{[}package
vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list
checks performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Gene Annotation with biomaRt yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/12-bioinformatics/biomart-annotation.Rmd\#for-reviewers}{\texttt{biomart-annotation}
--- For Reviewers}

\subsubsection{Gene Ontology Enrichment}\label{gene-ontology-enrichment}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Gene Ontology Enrichment} as implemented in \emph{{[}package
vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list
checks performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Gene Ontology Enrichment yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/12-bioinformatics/gene-ontology-enrichment.Rmd\#for-reviewers}{\texttt{gene-ontology-enrichment}
--- For Reviewers}

\subsubsection{GSEA Preranked Analysis}\label{gsea-preranked-analysis}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{GSEA Preranked Analysis} as implemented in \emph{{[}package
vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list
checks performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
GSEA Preranked Analysis yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/12-bioinformatics/gsea-preranked.Rmd\#for-reviewers}{\texttt{gsea-preranked}
--- For Reviewers}

\subsubsection{GSVA Single-Sample
Enrichment}\label{gsva-single-sample-enrichment}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{GSVA Single-Sample Enrichment} as implemented in
\emph{{[}package vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were
checked: \emph{{[}list checks performed{]}}. \emph{{[}Effect size with
95\% confidence interval reported as the primary outcome{]}}. Two-sided
p-values use \emph{α = 0.05}; \emph{{[}multiple-comparison adjustment if
any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
GSVA Single-Sample Enrichment yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/12-bioinformatics/gsva-single-sample.Rmd\#for-reviewers}{\texttt{gsva-single-sample}
--- For Reviewers}

\subsubsection{Heatmaps for RNA-seq}\label{heatmaps-for-rna-seq}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Heatmaps for RNA-seq} as implemented in \emph{{[}package
vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list
checks performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Heatmaps for RNA-seq yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/12-bioinformatics/heatmaps-rnaseq.Rmd\#for-reviewers}{\texttt{heatmaps-rnaseq}
--- For Reviewers}

\subsubsection{Integration with Harmony}\label{integration-with-harmony}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Integration with Harmony} as implemented in \emph{{[}package
vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list
checks performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Integration with Harmony yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/12-bioinformatics/scrna-integration-harmony.Rmd\#for-reviewers}{\texttt{scrna-integration-harmony}
--- For Reviewers}

\subsubsection{KEGG Pathway Enrichment}\label{kegg-pathway-enrichment}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{KEGG Pathway Enrichment} as implemented in \emph{{[}package
vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list
checks performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
KEGG Pathway Enrichment yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/12-bioinformatics/pathway-enrichment-kegg.Rmd\#for-reviewers}{\texttt{pathway-enrichment-kegg}
--- For Reviewers}

\subsubsection{MA Plots}\label{ma-plots}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using \textbf{MA
Plots} as implemented in \emph{{[}package vX.Y{]}} in R
\emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list checks
performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
MA Plots yielded \emph{{[}estimate{]}} (95\% CI \emph{{[}lower,
upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic / assumption
summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/12-bioinformatics/ma-plots.Rmd\#for-reviewers}{\texttt{ma-plots}
--- For Reviewers}

\subsubsection{Metagenomic Profiling}\label{metagenomic-profiling}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Metagenomic Profiling} as implemented in \emph{{[}package
vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list
checks performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Metagenomic Profiling yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/12-bioinformatics/metagenomic-profiling.Rmd\#for-reviewers}{\texttt{metagenomic-profiling}
--- For Reviewers}

\subsubsection{Microbiome Analysis with
DADA2}\label{microbiome-analysis-with-dada2}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Microbiome Analysis with DADA2} as implemented in
\emph{{[}package vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were
checked: \emph{{[}list checks performed{]}}. \emph{{[}Effect size with
95\% confidence interval reported as the primary outcome{]}}. Two-sided
p-values use \emph{α = 0.05}; \emph{{[}multiple-comparison adjustment if
any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Microbiome Analysis with DADA2 yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/12-bioinformatics/microbiome-dada2.Rmd\#for-reviewers}{\texttt{microbiome-dada2}
--- For Reviewers}

\subsubsection{Microbiome Diversity
Metrics}\label{microbiome-diversity-metrics}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Microbiome Diversity Metrics} as implemented in \emph{{[}package
vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list
checks performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Microbiome Diversity Metrics yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/12-bioinformatics/microbiome-diversity.Rmd\#for-reviewers}{\texttt{microbiome-diversity}
--- For Reviewers}

\subsubsection{Multi-Omics Integration}\label{multi-omics-integration}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Multi-Omics Integration} as implemented in \emph{{[}package
vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list
checks performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Multi-Omics Integration yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/12-bioinformatics/multiomics-integration.Rmd\#for-reviewers}{\texttt{multiomics-integration}
--- For Reviewers}

\subsubsection{Multiple Sequence
Alignment}\label{multiple-sequence-alignment}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Multiple Sequence Alignment} as implemented in \emph{{[}package
vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list
checks performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Multiple Sequence Alignment yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/12-bioinformatics/msa-biostrings.Rmd\#for-reviewers}{\texttt{msa-biostrings}
--- For Reviewers}

\subsubsection{PCA of RNA-seq Samples}\label{pca-of-rna-seq-samples}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{PCA of RNA-seq Samples} as implemented in \emph{{[}package
vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list
checks performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
PCA of RNA-seq Samples yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/12-bioinformatics/pca-of-rnaseq.Rmd\#for-reviewers}{\texttt{pca-of-rnaseq}
--- For Reviewers}

\subsubsection{Phylogenetic Trees with
ape}\label{phylogenetic-trees-with-ape}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Phylogenetic Trees with ape} as implemented in \emph{{[}package
vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list
checks performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Phylogenetic Trees with ape yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/12-bioinformatics/phylogenetic-trees-ape.Rmd\#for-reviewers}{\texttt{phylogenetic-trees-ape}
--- For Reviewers}

\subsubsection{Population Genetics
Basics}\label{population-genetics-basics}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Population Genetics Basics} as implemented in \emph{{[}package
vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list
checks performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Population Genetics Basics yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/12-bioinformatics/population-genetics-basics.Rmd\#for-reviewers}{\texttt{population-genetics-basics}
--- For Reviewers}

\subsubsection{Protein Structure
Prediction}\label{protein-structure-prediction}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Protein Structure Prediction} as implemented in \emph{{[}package
vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list
checks performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Protein Structure Prediction yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/12-bioinformatics/protein-structure-prediction.Rmd\#for-reviewers}{\texttt{protein-structure-prediction}
--- For Reviewers}

\subsubsection{Proteomics with MSstats}\label{proteomics-with-msstats}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Proteomics with MSstats} as implemented in \emph{{[}package
vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list
checks performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Proteomics with MSstats yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/12-bioinformatics/proteomics-msstats.Rmd\#for-reviewers}{\texttt{proteomics-msstats}
--- For Reviewers}

\subsubsection{Pseudoalignment with Salmon and
Kallisto}\label{pseudoalignment-with-salmon-and-kallisto}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Pseudoalignment with Salmon and Kallisto} as implemented in
\emph{{[}package vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were
checked: \emph{{[}list checks performed{]}}. \emph{{[}Effect size with
95\% confidence interval reported as the primary outcome{]}}. Two-sided
p-values use \emph{α = 0.05}; \emph{{[}multiple-comparison adjustment if
any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Pseudoalignment with Salmon and Kallisto yielded \emph{{[}estimate{]}}
(95\% CI \emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}).
\emph{{[}Diagnostic / assumption summary{]}}. \emph{{[}Sensitivity
analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/12-bioinformatics/salmon-kallisto-pseudoalignment.Rmd\#for-reviewers}{\texttt{salmon-kallisto-pseudoalignment}
--- For Reviewers}

\subsubsection{Read Trimming and Adapter
Removal}\label{read-trimming-and-adapter-removal}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Read Trimming and Adapter Removal} as implemented in
\emph{{[}package vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were
checked: \emph{{[}list checks performed{]}}. \emph{{[}Effect size with
95\% confidence interval reported as the primary outcome{]}}. Two-sided
p-values use \emph{α = 0.05}; \emph{{[}multiple-comparison adjustment if
any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Read Trimming and Adapter Removal yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/12-bioinformatics/read-trimming-adapters.Rmd\#for-reviewers}{\texttt{read-trimming-adapters}
--- For Reviewers}

\subsubsection{RNA-seq Normalisation}\label{rna-seq-normalisation}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{RNA-seq Normalisation} as implemented in \emph{{[}package
vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list
checks performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
RNA-seq Normalisation yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/12-bioinformatics/rnaseq-normalization.Rmd\#for-reviewers}{\texttt{rnaseq-normalization}
--- For Reviewers}

\subsubsection{scRNA Clustering}\label{scrna-clustering}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{scRNA Clustering} as implemented in \emph{{[}package vX.Y{]}} in
R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list checks
performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
scRNA Clustering yielded \emph{{[}estimate{]}} (95\% CI \emph{{[}lower,
upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic / assumption
summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/12-bioinformatics/scrna-clustering.Rmd\#for-reviewers}{\texttt{scrna-clustering}
--- For Reviewers}

\subsubsection{scRNA Normalisation}\label{scrna-normalisation}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{scRNA Normalisation} as implemented in \emph{{[}package vX.Y{]}}
in R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list checks
performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
scRNA Normalisation yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/12-bioinformatics/scrna-normalization.Rmd\#for-reviewers}{\texttt{scrna-normalization}
--- For Reviewers}

\subsubsection{scRNA QC and Filtering}\label{scrna-qc-and-filtering}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{scRNA QC and Filtering} as implemented in \emph{{[}package
vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list
checks performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
scRNA QC and Filtering yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/12-bioinformatics/scrna-qc-filtering.Rmd\#for-reviewers}{\texttt{scrna-qc-filtering}
--- For Reviewers}

\subsubsection{Sequence Alignment:
Overview}\label{sequence-alignment-overview}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Sequence Alignment: Overview} as implemented in \emph{{[}package
vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list
checks performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Sequence Alignment: Overview yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/12-bioinformatics/sequence-alignment-overview.Rmd\#for-reviewers}{\texttt{sequence-alignment-overview}
--- For Reviewers}

\subsubsection{Single-Cell with Seurat}\label{single-cell-with-seurat}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Single-Cell with Seurat} as implemented in \emph{{[}package
vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list
checks performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Single-Cell with Seurat yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/12-bioinformatics/single-cell-seurat-intro.Rmd\#for-reviewers}{\texttt{single-cell-seurat-intro}
--- For Reviewers}

\subsubsection{Spatial Transcriptomics}\label{spatial-transcriptomics}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Spatial Transcriptomics} as implemented in \emph{{[}package
vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list
checks performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Spatial Transcriptomics yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/12-bioinformatics/spatial-transcriptomics.Rmd\#for-reviewers}{\texttt{spatial-transcriptomics}
--- For Reviewers}

\subsubsection{STAR: Spliced Alignment}\label{star-spliced-alignment}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{STAR: Spliced Alignment} as implemented in \emph{{[}package
vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list
checks performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
STAR: Spliced Alignment yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/12-bioinformatics/star-spliced-alignment.Rmd\#for-reviewers}{\texttt{star-spliced-alignment}
--- For Reviewers}

\subsubsection{Structural Variant
Detection}\label{structural-variant-detection}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Structural Variant Detection} as implemented in \emph{{[}package
vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list
checks performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Structural Variant Detection yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/12-bioinformatics/structural-variant-detection.Rmd\#for-reviewers}{\texttt{structural-variant-detection}
--- For Reviewers}

\subsubsection{Surrogate Variable Analysis
(SVA)}\label{surrogate-variable-analysis-sva}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Surrogate Variable Analysis (SVA)} as implemented in
\emph{{[}package vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were
checked: \emph{{[}list checks performed{]}}. \emph{{[}Effect size with
95\% confidence interval reported as the primary outcome{]}}. Two-sided
p-values use \emph{α = 0.05}; \emph{{[}multiple-comparison adjustment if
any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Surrogate Variable Analysis (SVA) yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/12-bioinformatics/surrogate-variable-analysis-sva.Rmd\#for-reviewers}{\texttt{surrogate-variable-analysis-sva}
--- For Reviewers}

\subsubsection{Trajectory Analysis}\label{trajectory-analysis}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Trajectory Analysis} as implemented in \emph{{[}package vX.Y{]}}
in R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list checks
performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Trajectory Analysis yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/12-bioinformatics/scrna-trajectory-analysis.Rmd\#for-reviewers}{\texttt{scrna-trajectory-analysis}
--- For Reviewers}

\subsubsection{Transcript-to-Gene
Summarisation}\label{transcript-to-gene-summarisation}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Transcript-to-Gene Summarisation} as implemented in
\emph{{[}package vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were
checked: \emph{{[}list checks performed{]}}. \emph{{[}Effect size with
95\% confidence interval reported as the primary outcome{]}}. Two-sided
p-values use \emph{α = 0.05}; \emph{{[}multiple-comparison adjustment if
any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Transcript-to-Gene Summarisation yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/12-bioinformatics/tx-to-gene-summarization.Rmd\#for-reviewers}{\texttt{tx-to-gene-summarization}
--- For Reviewers}

\subsubsection{Variant Annotation with
VEP}\label{variant-annotation-with-vep}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Variant Annotation with VEP} as implemented in \emph{{[}package
vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list
checks performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Variant Annotation with VEP yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/12-bioinformatics/annotation-variant-vep.Rmd\#for-reviewers}{\texttt{annotation-variant-vep}
--- For Reviewers}

\subsubsection{Variant Calling with
GATK}\label{variant-calling-with-gatk}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Variant Calling with GATK} as implemented in \emph{{[}package
vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list
checks performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Variant Calling with GATK yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/12-bioinformatics/variant-calling-gatk.Rmd\#for-reviewers}{\texttt{variant-calling-gatk}
--- For Reviewers}

\subsubsection{VCF Manipulation}\label{vcf-manipulation}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{VCF Manipulation} as implemented in \emph{{[}package vX.Y{]}} in
R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list checks
performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
VCF Manipulation yielded \emph{{[}estimate{]}} (95\% CI \emph{{[}lower,
upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic / assumption
summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/12-bioinformatics/vcf-manipulation.Rmd\#for-reviewers}{\texttt{vcf-manipulation}
--- For Reviewers}

\subsubsection{Volcano Plots}\label{volcano-plots}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Volcano Plots} as implemented in \emph{{[}package vX.Y{]}} in R
\emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list checks
performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Volcano Plots yielded \emph{{[}estimate{]}} (95\% CI \emph{{[}lower,
upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic / assumption
summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/12-bioinformatics/volcano-plots.Rmd\#for-reviewers}{\texttt{volcano-plots}
--- For Reviewers}

\end{document}
