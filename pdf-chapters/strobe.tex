% Options for packages loaded elsewhere
\PassOptionsToPackage{unicode}{hyperref}
\PassOptionsToPackage{hyphens}{url}
\documentclass[
]{article}
\usepackage{xcolor}
\usepackage[margin=1in]{geometry}
\usepackage{amsmath,amssymb}
\setcounter{secnumdepth}{-\maxdimen} % remove section numbering
\usepackage{iftex}
\ifPDFTeX
  \usepackage[T1]{fontenc}
  \usepackage[utf8]{inputenc}
  \usepackage{textcomp} % provide euro and other symbols
\else % if luatex or xetex
  \usepackage{unicode-math} % this also loads fontspec
  \defaultfontfeatures{Scale=MatchLowercase}
  \defaultfontfeatures[\rmfamily]{Ligatures=TeX,Scale=1}
\fi
\usepackage{lmodern}
\ifPDFTeX\else
  % xetex/luatex font selection
\fi
% Use upquote if available, for straight quotes in verbatim environments
\IfFileExists{upquote.sty}{\usepackage{upquote}}{}
\IfFileExists{microtype.sty}{% use microtype if available
  \usepackage[]{microtype}
  \UseMicrotypeSet[protrusion]{basicmath} % disable protrusion for tt fonts
}{}
\makeatletter
\@ifundefined{KOMAClassName}{% if non-KOMA class
  \IfFileExists{parskip.sty}{%
    \usepackage{parskip}
  }{% else
    \setlength{\parindent}{0pt}
    \setlength{\parskip}{6pt plus 2pt minus 1pt}}
}{% if KOMA class
  \KOMAoptions{parskip=half}}
\makeatother
\usepackage{graphicx}
\makeatletter
\newsavebox\pandoc@box
\newcommand*\pandocbounded[1]{% scales image to fit in text height/width
  \sbox\pandoc@box{#1}%
  \Gscale@div\@tempa{\textheight}{\dimexpr\ht\pandoc@box+\dp\pandoc@box\relax}%
  \Gscale@div\@tempb{\linewidth}{\wd\pandoc@box}%
  \ifdim\@tempb\p@<\@tempa\p@\let\@tempa\@tempb\fi% select the smaller of both
  \ifdim\@tempa\p@<\p@\scalebox{\@tempa}{\usebox\pandoc@box}%
  \else\usebox{\pandoc@box}%
  \fi%
}
% Set default figure placement to htbp
\def\fps@figure{htbp}
\makeatother
\setlength{\emergencystretch}{3em} % prevent overfull lines
\providecommand{\tightlist}{%
  \setlength{\itemsep}{0pt}\setlength{\parskip}{0pt}}
\usepackage{bookmark}
\IfFileExists{xurl.sty}{\usepackage{xurl}}{} % add URL line breaks if available
\urlstyle{same}
\hypersetup{
  pdftitle={STROBE --- observational studies},
  hidelinks,
  pdfcreator={LaTeX via pandoc}}

\title{STROBE --- observational studies}
\author{}
\date{\vspace{-2.5em}}

\begin{document}
\maketitle

\section{STROBE --- observational
studies}\label{strobe-observational-studies}

STROBE --- \emph{Strengthening the Reporting of Observational Studies in
Epidemiology} --- covers cohort, case-control, and cross-sectional
studies. The 22-item checklist is at
\url{https://www.strobe-statement.org/}. The templates below cover the
analytic methods you are most likely to use in a STROBE-aligned paper.

This page contains \textbf{186 method-specific templates}. Each is a
skeleton with placeholders in italics; the prose is written so you can
paste it into a manuscript and replace only the bracketed values.

\subsubsection{2\^{}k Factorial Designs}\label{k-factorial-designs}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{2\^{}k Factorial Designs} as implemented in \emph{{[}package
vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list
checks performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
2\^{}k Factorial Designs yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/16-design/factorial-2k.Rmd\#for-reviewers}{\texttt{factorial-2k}
--- For Reviewers}

\subsubsection{3\^{}k Factorial Designs}\label{k-factorial-designs-1}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{3\^{}k Factorial Designs} as implemented in \emph{{[}package
vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list
checks performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
3\^{}k Factorial Designs yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/16-design/factorial-3k.Rmd\#for-reviewers}{\texttt{factorial-3k}
--- For Reviewers}

\subsubsection{Accelerated Failure Time
Models}\label{accelerated-failure-time-models}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Accelerated Failure Time Models} as implemented in
\emph{{[}package vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were
checked: \emph{{[}list checks performed{]}}. \emph{{[}Effect size with
95\% confidence interval reported as the primary outcome{]}}. Two-sided
p-values use \emph{α = 0.05}; \emph{{[}multiple-comparison adjustment if
any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Accelerated Failure Time Models yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/11-survival/accelerated-failure-time.Rmd\#for-reviewers}{\texttt{accelerated-failure-time}
--- For Reviewers}

\subsubsection{Added-Variable Plots}\label{added-variable-plots}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Added-Variable Plots} as implemented in \emph{{[}package
vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list
checks performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Added-Variable Plots yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/07-regression/added-variable-plots.Rmd\#for-reviewers}{\texttt{added-variable-plots}
--- For Reviewers}

\subsubsection{Anderson-Darling Test}\label{anderson-darling-test}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Anderson-Darling Test} as implemented in \emph{{[}package
vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list
checks performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Anderson-Darling Test yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/04-inferential/anderson-darling-test.Rmd\#for-reviewers}{\texttt{anderson-darling-test}
--- For Reviewers}

\subsubsection{Balanced Incomplete Block
Designs}\label{balanced-incomplete-block-designs}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Balanced Incomplete Block Designs} as implemented in
\emph{{[}package vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were
checked: \emph{{[}list checks performed{]}}. \emph{{[}Effect size with
95\% confidence interval reported as the primary outcome{]}}. Two-sided
p-values use \emph{α = 0.05}; \emph{{[}multiple-comparison adjustment if
any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Balanced Incomplete Block Designs yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/16-design/balanced-incomplete-block.Rmd\#for-reviewers}{\texttt{balanced-incomplete-block}
--- For Reviewers}

\subsubsection{Bartlett's Test}\label{bartletts-test}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Bartlett's Test} as implemented in \emph{{[}package vX.Y{]}} in
R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list checks
performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Bartlett's Test yielded \emph{{[}estimate{]}} (95\% CI \emph{{[}lower,
upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic / assumption
summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/04-inferential/bartlett-test.Rmd\#for-reviewers}{\texttt{bartlett-test}
--- For Reviewers}

\subsubsection{Benjamini-Hochberg FDR}\label{benjamini-hochberg-fdr}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Benjamini-Hochberg FDR} as implemented in \emph{{[}package
vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list
checks performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Benjamini-Hochberg FDR yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/04-inferential/benjamini-hochberg-fdr.Rmd\#for-reviewers}{\texttt{benjamini-hochberg-fdr}
--- For Reviewers}

\subsubsection{Benjamini-Yekutieli FDR}\label{benjamini-yekutieli-fdr}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Benjamini-Yekutieli FDR} as implemented in \emph{{[}package
vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list
checks performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Benjamini-Yekutieli FDR yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/04-inferential/benjamini-yekutieli.Rmd\#for-reviewers}{\texttt{benjamini-yekutieli}
--- For Reviewers}

\subsubsection{Best Subset Selection}\label{best-subset-selection}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Best Subset Selection} as implemented in \emph{{[}package
vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list
checks performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Best Subset Selection yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/07-regression/best-subset-selection.Rmd\#for-reviewers}{\texttt{best-subset-selection}
--- For Reviewers}

\subsubsection{Beta Regression}\label{beta-regression}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Beta Regression} as implemented in \emph{{[}package vX.Y{]}} in
R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list checks
performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Beta Regression yielded \emph{{[}estimate{]}} (95\% CI \emph{{[}lower,
upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic / assumption
summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/07-regression/beta-regression.Rmd\#for-reviewers}{\texttt{beta-regression}
--- For Reviewers}

\subsubsection{Bonferroni Correction}\label{bonferroni-correction}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Bonferroni Correction} as implemented in \emph{{[}package
vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list
checks performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Bonferroni Correction yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/04-inferential/bonferroni-correction.Rmd\#for-reviewers}{\texttt{bonferroni-correction}
--- For Reviewers}

\subsubsection{Bootstrap Confidence
Intervals}\label{bootstrap-confidence-intervals}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Bootstrap Confidence Intervals} as implemented in
\emph{{[}package vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were
checked: \emph{{[}list checks performed{]}}. \emph{{[}Effect size with
95\% confidence interval reported as the primary outcome{]}}. Two-sided
p-values use \emph{α = 0.05}; \emph{{[}multiple-comparison adjustment if
any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Bootstrap Confidence Intervals yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/04-inferential/bootstrap-confidence-intervals.Rmd\#for-reviewers}{\texttt{bootstrap-confidence-intervals}
--- For Reviewers}

\subsubsection{Box-Behnken Designs}\label{box-behnken-designs}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Box-Behnken Designs} as implemented in \emph{{[}package vX.Y{]}}
in R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list checks
performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Box-Behnken Designs yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/16-design/box-behnken-design.Rmd\#for-reviewers}{\texttt{box-behnken-design}
--- For Reviewers}

\subsubsection{Censoring Types}\label{censoring-types}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Censoring Types} as implemented in \emph{{[}package vX.Y{]}} in
R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list checks
performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Censoring Types yielded \emph{{[}estimate{]}} (95\% CI \emph{{[}lower,
upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic / assumption
summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/11-survival/censoring-types.Rmd\#for-reviewers}{\texttt{censoring-types}
--- For Reviewers}

\subsubsection{Central Composite
Designs}\label{central-composite-designs}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Central Composite Designs} as implemented in \emph{{[}package
vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list
checks performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Central Composite Designs yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/16-design/central-composite-design.Rmd\#for-reviewers}{\texttt{central-composite-design}
--- For Reviewers}

\subsubsection{Centring and Scaling
Predictors}\label{centring-and-scaling-predictors}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Centring and Scaling Predictors} as implemented in
\emph{{[}package vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were
checked: \emph{{[}list checks performed{]}}. \emph{{[}Effect size with
95\% confidence interval reported as the primary outcome{]}}. Two-sided
p-values use \emph{α = 0.05}; \emph{{[}multiple-comparison adjustment if
any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Centring and Scaling Predictors yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/07-regression/centered-scaled-predictors.Rmd\#for-reviewers}{\texttt{centered-scaled-predictors}
--- For Reviewers}

\subsubsection{Checking Cox Assumptions}\label{checking-cox-assumptions}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Checking Cox Assumptions} as implemented in \emph{{[}package
vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list
checks performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Checking Cox Assumptions yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/11-survival/cox-assumptions-check.Rmd\#for-reviewers}{\texttt{cox-assumptions-check}
--- For Reviewers}

\subsubsection{Chi-Squared Goodness-of-Fit
Test}\label{chi-squared-goodness-of-fit-test}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Chi-Squared Goodness-of-Fit Test} as implemented in
\emph{{[}package vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were
checked: \emph{{[}list checks performed{]}}. \emph{{[}Effect size with
95\% confidence interval reported as the primary outcome{]}}. Two-sided
p-values use \emph{α = 0.05}; \emph{{[}multiple-comparison adjustment if
any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Chi-Squared Goodness-of-Fit Test yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/04-inferential/chi-squared-goodness-of-fit.Rmd\#for-reviewers}{\texttt{chi-squared-goodness-of-fit}
--- For Reviewers}

\subsubsection{Chi-Squared Test of
Independence}\label{chi-squared-test-of-independence}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Chi-Squared Test of Independence} as implemented in
\emph{{[}package vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were
checked: \emph{{[}list checks performed{]}}. \emph{{[}Effect size with
95\% confidence interval reported as the primary outcome{]}}. Two-sided
p-values use \emph{α = 0.05}; \emph{{[}multiple-comparison adjustment if
any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Chi-Squared Test of Independence yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/04-inferential/chi-squared-independence.Rmd\#for-reviewers}{\texttt{chi-squared-independence}
--- For Reviewers}

\subsubsection{Cochran-Mantel-Haenszel
Test}\label{cochran-mantel-haenszel-test}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Cochran-Mantel-Haenszel Test} as implemented in \emph{{[}package
vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list
checks performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Cochran-Mantel-Haenszel Test yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/04-inferential/cochran-mantel-haenszel.Rmd\#for-reviewers}{\texttt{cochran-mantel-haenszel}
--- For Reviewers}

\subsubsection{Competing Risks: Cumulative
Incidence}\label{competing-risks-cumulative-incidence}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Competing Risks: Cumulative Incidence} as implemented in
\emph{{[}package vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were
checked: \emph{{[}list checks performed{]}}. \emph{{[}Effect size with
95\% confidence interval reported as the primary outcome{]}}. Two-sided
p-values use \emph{α = 0.05}; \emph{{[}multiple-comparison adjustment if
any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Competing Risks: Cumulative Incidence yielded \emph{{[}estimate{]}}
(95\% CI \emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}).
\emph{{[}Diagnostic / assumption summary{]}}. \emph{{[}Sensitivity
analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/11-survival/competing-risks-cif.Rmd\#for-reviewers}{\texttt{competing-risks-cif}
--- For Reviewers}

\subsubsection{Completely Randomised
Design}\label{completely-randomised-design}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Completely Randomised Design} as implemented in \emph{{[}package
vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list
checks performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Completely Randomised Design yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/16-design/completely-randomized-design.Rmd\#for-reviewers}{\texttt{completely-randomized-design}
--- For Reviewers}

\subsubsection{Concordance and the
C-Index}\label{concordance-and-the-c-index}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Concordance and the C-Index} as implemented in \emph{{[}package
vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list
checks performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Concordance and the C-Index yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/11-survival/concordance-c-index.Rmd\#for-reviewers}{\texttt{concordance-c-index}
--- For Reviewers}

\subsubsection{Conditional Survival}\label{conditional-survival}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Conditional Survival} as implemented in \emph{{[}package
vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list
checks performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Conditional Survival yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/11-survival/conditional-survival.Rmd\#for-reviewers}{\texttt{conditional-survival}
--- For Reviewers}

\subsubsection{Constrained Mixture
Designs}\label{constrained-mixture-designs}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Constrained Mixture Designs} as implemented in \emph{{[}package
vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list
checks performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Constrained Mixture Designs yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/16-design/constrained-mixture.Rmd\#for-reviewers}{\texttt{constrained-mixture}
--- For Reviewers}

\subsubsection{Contrasts in R}\label{contrasts-in-r}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Contrasts in R} as implemented in \emph{{[}package vX.Y{]}} in R
\emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list checks
performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Contrasts in R yielded \emph{{[}estimate{]}} (95\% CI \emph{{[}lower,
upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic / assumption
summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/07-regression/contrasts-in-r.Rmd\#for-reviewers}{\texttt{contrasts-in-r}
--- For Reviewers}

\subsubsection{Cook's Distance}\label{cooks-distance}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Cook's Distance} as implemented in \emph{{[}package vX.Y{]}} in
R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list checks
performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Cook's Distance yielded \emph{{[}estimate{]}} (95\% CI \emph{{[}lower,
upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic / assumption
summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/07-regression/cooks-distance.Rmd\#for-reviewers}{\texttt{cooks-distance}
--- For Reviewers}

\subsubsection{Cox Proportional Hazards
Regression}\label{cox-proportional-hazards-regression}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Cox Proportional Hazards Regression} as implemented in
\emph{{[}package vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were
checked: \emph{{[}list checks performed{]}}. \emph{{[}Effect size with
95\% confidence interval reported as the primary outcome{]}}. Two-sided
p-values use \emph{α = 0.05}; \emph{{[}multiple-comparison adjustment if
any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Cox Proportional Hazards Regression yielded \emph{{[}estimate{]}} (95\%
CI \emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}).
\emph{{[}Diagnostic / assumption summary{]}}. \emph{{[}Sensitivity
analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/11-survival/cox-proportional-hazards.Rmd\#for-reviewers}{\texttt{cox-proportional-hazards}
--- For Reviewers}

\subsubsection{Cox Regression as
Regression}\label{cox-regression-as-regression}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Cox Regression as Regression} as implemented in \emph{{[}package
vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list
checks performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Cox Regression as Regression yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/07-regression/survival-regression-cox.Rmd\#for-reviewers}{\texttt{survival-regression-cox}
--- For Reviewers}

\subsubsection{Crossover Designs}\label{crossover-designs}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Crossover Designs} as implemented in \emph{{[}package vX.Y{]}}
in R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list checks
performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Crossover Designs yielded \emph{{[}estimate{]}} (95\% CI \emph{{[}lower,
upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic / assumption
summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/16-design/crossover-designs.Rmd\#for-reviewers}{\texttt{crossover-designs}
--- For Reviewers}

\subsubsection{Desirability Functions}\label{desirability-functions}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Desirability Functions} as implemented in \emph{{[}package
vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list
checks performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Desirability Functions yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/16-design/desirability-function.Rmd\#for-reviewers}{\texttt{desirability-function}
--- For Reviewers}

\subsubsection{Dirichlet Regression}\label{dirichlet-regression}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Dirichlet Regression} as implemented in \emph{{[}package
vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list
checks performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Dirichlet Regression yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/07-regression/dirichlet-regression.Rmd\#for-reviewers}{\texttt{dirichlet-regression}
--- For Reviewers}

\subsubsection{Dummy Coding}\label{dummy-coding}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Dummy Coding} as implemented in \emph{{[}package vX.Y{]}} in R
\emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list checks
performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Dummy Coding yielded \emph{{[}estimate{]}} (95\% CI \emph{{[}lower,
upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic / assumption
summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/07-regression/dummy-coding.Rmd\#for-reviewers}{\texttt{dummy-coding}
--- For Reviewers}

\subsubsection{Effect (Deviation) Coding}\label{effect-deviation-coding}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Effect (Deviation) Coding} as implemented in \emph{{[}package
vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list
checks performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Effect (Deviation) Coding yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/07-regression/effect-coding.Rmd\#for-reviewers}{\texttt{effect-coding}
--- For Reviewers}

\subsubsection{Effect Size: Cohen's d}\label{effect-size-cohens-d}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Effect Size: Cohen's d} as implemented in \emph{{[}package
vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list
checks performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Effect Size: Cohen's d yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/05-power/effect-size-cohens-d.Rmd\#for-reviewers}{\texttt{effect-size-cohens-d}
--- For Reviewers}

\subsubsection{Effect Size: Cohen's h}\label{effect-size-cohens-h}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Effect Size: Cohen's h} as implemented in \emph{{[}package
vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list
checks performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Effect Size: Cohen's h yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/05-power/effect-size-cohens-h.Rmd\#for-reviewers}{\texttt{effect-size-cohens-h}
--- For Reviewers}

\subsubsection{Effect Size: Eta-Squared}\label{effect-size-eta-squared}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Effect Size: Eta-Squared} as implemented in \emph{{[}package
vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list
checks performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Effect Size: Eta-Squared yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/05-power/effect-size-eta-squared.Rmd\#for-reviewers}{\texttt{effect-size-eta-squared}
--- For Reviewers}

\subsubsection{Effect Sizes: Overview}\label{effect-sizes-overview}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Effect Sizes: Overview} as implemented in \emph{{[}package
vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list
checks performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Effect Sizes: Overview yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/04-inferential/effect-sizes-overview.Rmd\#for-reviewers}{\texttt{effect-sizes-overview}
--- For Reviewers}

\subsubsection{Elastic Net}\label{elastic-net}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Elastic Net} as implemented in \emph{{[}package vX.Y{]}} in R
\emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list checks
performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Elastic Net yielded \emph{{[}estimate{]}} (95\% CI \emph{{[}lower,
upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic / assumption
summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/07-regression/elastic-net.Rmd\#for-reviewers}{\texttt{elastic-net}
--- For Reviewers}

\subsubsection{Equivalence Testing with
TOST}\label{equivalence-testing-with-tost}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Equivalence Testing with TOST} as implemented in
\emph{{[}package vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were
checked: \emph{{[}list checks performed{]}}. \emph{{[}Effect size with
95\% confidence interval reported as the primary outcome{]}}. Two-sided
p-values use \emph{α = 0.05}; \emph{{[}multiple-comparison adjustment if
any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Equivalence Testing with TOST yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/04-inferential/equivalence-tost.Rmd\#for-reviewers}{\texttt{equivalence-tost}
--- For Reviewers}

\subsubsection{Fine-Gray Subdistribution
Hazards}\label{fine-gray-subdistribution-hazards}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Fine-Gray Subdistribution Hazards} as implemented in
\emph{{[}package vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were
checked: \emph{{[}list checks performed{]}}. \emph{{[}Effect size with
95\% confidence interval reported as the primary outcome{]}}. Two-sided
p-values use \emph{α = 0.05}; \emph{{[}multiple-comparison adjustment if
any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Fine-Gray Subdistribution Hazards yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/11-survival/fine-gray-regression.Rmd\#for-reviewers}{\texttt{fine-gray-regression}
--- For Reviewers}

\subsubsection{Fisher's Exact Test}\label{fishers-exact-test}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Fisher's Exact Test} as implemented in \emph{{[}package vX.Y{]}}
in R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list checks
performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Fisher's Exact Test yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/04-inferential/fisher-exact-test.Rmd\#for-reviewers}{\texttt{fisher-exact-test}
--- For Reviewers}

\subsubsection{Fractional Factorial
Designs}\label{fractional-factorial-designs}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Fractional Factorial Designs} as implemented in \emph{{[}package
vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list
checks performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Fractional Factorial Designs yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/16-design/fractional-factorial-design.Rmd\#for-reviewers}{\texttt{fractional-factorial-design}
--- For Reviewers}

\subsubsection{Frailty Models}\label{frailty-models}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Frailty Models} as implemented in \emph{{[}package vX.Y{]}} in R
\emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list checks
performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Frailty Models yielded \emph{{[}estimate{]}} (95\% CI \emph{{[}lower,
upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic / assumption
summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/11-survival/frailty-models.Rmd\#for-reviewers}{\texttt{frailty-models}
--- For Reviewers}

\subsubsection{Friedman Test}\label{friedman-test}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Friedman Test} as implemented in \emph{{[}package vX.Y{]}} in R
\emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list checks
performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Friedman Test yielded \emph{{[}estimate{]}} (95\% CI \emph{{[}lower,
upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic / assumption
summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/04-inferential/friedman-test.Rmd\#for-reviewers}{\texttt{friedman-test}
--- For Reviewers}

\subsubsection{GAMMs: Additive Mixed
Models}\label{gamms-additive-mixed-models}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{GAMMs: Additive Mixed Models} as implemented in \emph{{[}package
vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list
checks performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
GAMMs: Additive Mixed Models yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/07-regression/gamm-additive-mixed.Rmd\#for-reviewers}{\texttt{gamm-additive-mixed}
--- For Reviewers}

\subsubsection{GAMs: Introduction}\label{gams-introduction}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{GAMs: Introduction} as implemented in \emph{{[}package vX.Y{]}}
in R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list checks
performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
GAMs: Introduction yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/07-regression/gam-introduction.Rmd\#for-reviewers}{\texttt{gam-introduction}
--- For Reviewers}

\subsubsection{Generalised Estimating
Equations}\label{generalised-estimating-equations}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Generalised Estimating Equations} as implemented in
\emph{{[}package vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were
checked: \emph{{[}list checks performed{]}}. \emph{{[}Effect size with
95\% confidence interval reported as the primary outcome{]}}. Two-sided
p-values use \emph{α = 0.05}; \emph{{[}multiple-comparison adjustment if
any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Generalised Estimating Equations yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/07-regression/generalized-estimating-equations.Rmd\#for-reviewers}{\texttt{generalized-estimating-equations}
--- For Reviewers}

\subsubsection{glmer: Generalised Mixed
Models}\label{glmer-generalised-mixed-models}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{glmer: Generalised Mixed Models} as implemented in
\emph{{[}package vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were
checked: \emph{{[}list checks performed{]}}. \emph{{[}Effect size with
95\% confidence interval reported as the primary outcome{]}}. Two-sided
p-values use \emph{α = 0.05}; \emph{{[}multiple-comparison adjustment if
any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
glmer: Generalised Mixed Models yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/07-regression/glmer-generalized-mixed.Rmd\#for-reviewers}{\texttt{glmer-generalized-mixed}
--- For Reviewers}

\subsubsection{Graeco-Latin Square
Designs}\label{graeco-latin-square-designs}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Graeco-Latin Square Designs} as implemented in \emph{{[}package
vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list
checks performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Graeco-Latin Square Designs yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/16-design/graeco-latin-square.Rmd\#for-reviewers}{\texttt{graeco-latin-square}
--- For Reviewers}

\subsubsection{Hazard, Survival, and Cumulative
Hazard}\label{hazard-survival-and-cumulative-hazard}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Hazard, Survival, and Cumulative Hazard} as implemented in
\emph{{[}package vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were
checked: \emph{{[}list checks performed{]}}. \emph{{[}Effect size with
95\% confidence interval reported as the primary outcome{]}}. Two-sided
p-values use \emph{α = 0.05}; \emph{{[}multiple-comparison adjustment if
any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Hazard, Survival, and Cumulative Hazard yielded \emph{{[}estimate{]}}
(95\% CI \emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}).
\emph{{[}Diagnostic / assumption summary{]}}. \emph{{[}Sensitivity
analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/11-survival/hazard-survival-cumulative.Rmd\#for-reviewers}{\texttt{hazard-survival-cumulative}
--- For Reviewers}

\subsubsection{Holm's Step-Down
Correction}\label{holms-step-down-correction}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Holm's Step-Down Correction} as implemented in \emph{{[}package
vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list
checks performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Holm's Step-Down Correction yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/04-inferential/holm-correction.Rmd\#for-reviewers}{\texttt{holm-correction}
--- For Reviewers}

\subsubsection{Hurdle Models}\label{hurdle-models}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Hurdle Models} as implemented in \emph{{[}package vX.Y{]}} in R
\emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list checks
performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Hurdle Models yielded \emph{{[}estimate{]}} (95\% CI \emph{{[}lower,
upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic / assumption
summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/07-regression/hurdle-models.Rmd\#for-reviewers}{\texttt{hurdle-models}
--- For Reviewers}

\subsubsection{Interactions in
Regression}\label{interactions-in-regression}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Interactions in Regression} as implemented in \emph{{[}package
vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list
checks performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Interactions in Regression yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/07-regression/interactions-in-regression.Rmd\#for-reviewers}{\texttt{interactions-in-regression}
--- For Reviewers}

\subsubsection{Interval-Censored Data}\label{interval-censored-data}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Interval-Censored Data} as implemented in \emph{{[}package
vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list
checks performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Interval-Censored Data yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/11-survival/interval-censored-data.Rmd\#for-reviewers}{\texttt{interval-censored-data}
--- For Reviewers}

\subsubsection{Joint Longitudinal-Survival
Models}\label{joint-longitudinal-survival-models}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Joint Longitudinal-Survival Models} as implemented in
\emph{{[}package vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were
checked: \emph{{[}list checks performed{]}}. \emph{{[}Effect size with
95\% confidence interval reported as the primary outcome{]}}. Two-sided
p-values use \emph{α = 0.05}; \emph{{[}multiple-comparison adjustment if
any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Joint Longitudinal-Survival Models yielded \emph{{[}estimate{]}} (95\%
CI \emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}).
\emph{{[}Diagnostic / assumption summary{]}}. \emph{{[}Sensitivity
analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/11-survival/joint-longitudinal-survival.Rmd\#for-reviewers}{\texttt{joint-longitudinal-survival}
--- For Reviewers}

\subsubsection{Kaplan-Meier Estimation}\label{kaplan-meier-estimation}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Kaplan-Meier Estimation} as implemented in \emph{{[}package
vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list
checks performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Kaplan-Meier Estimation yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/11-survival/kaplan-meier.Rmd\#for-reviewers}{\texttt{kaplan-meier}
--- For Reviewers}

\subsubsection{Kendall's Tau}\label{kendalls-tau}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Kendall's Tau} as implemented in \emph{{[}package vX.Y{]}} in R
\emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list checks
performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Kendall's Tau yielded \emph{{[}estimate{]}} (95\% CI \emph{{[}lower,
upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic / assumption
summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/04-inferential/kendall-tau.Rmd\#for-reviewers}{\texttt{kendall-tau}
--- For Reviewers}

\subsubsection{Kolmogorov-Smirnov Test}\label{kolmogorov-smirnov-test}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Kolmogorov-Smirnov Test} as implemented in \emph{{[}package
vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list
checks performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Kolmogorov-Smirnov Test yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/04-inferential/kolmogorov-smirnov-test.Rmd\#for-reviewers}{\texttt{kolmogorov-smirnov-test}
--- For Reviewers}

\subsubsection{Kruskal-Wallis Test}\label{kruskal-wallis-test}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Kruskal-Wallis Test} as implemented in \emph{{[}package vX.Y{]}}
in R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list checks
performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Kruskal-Wallis Test yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/04-inferential/kruskal-wallis.Rmd\#for-reviewers}{\texttt{kruskal-wallis}
--- For Reviewers}

\subsubsection{Landmark Analysis}\label{landmark-analysis}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Landmark Analysis} as implemented in \emph{{[}package vX.Y{]}}
in R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list checks
performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Landmark Analysis yielded \emph{{[}estimate{]}} (95\% CI \emph{{[}lower,
upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic / assumption
summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/11-survival/landmark-analysis.Rmd\#for-reviewers}{\texttt{landmark-analysis}
--- For Reviewers}

\subsubsection{Lasso Regression}\label{lasso-regression}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Lasso Regression} as implemented in \emph{{[}package vX.Y{]}} in
R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list checks
performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Lasso Regression yielded \emph{{[}estimate{]}} (95\% CI \emph{{[}lower,
upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic / assumption
summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/07-regression/lasso-regression.Rmd\#for-reviewers}{\texttt{lasso-regression}
--- For Reviewers}

\subsubsection{Latin Square Designs}\label{latin-square-designs}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Latin Square Designs} as implemented in \emph{{[}package
vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list
checks performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Latin Square Designs yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/16-design/latin-square.Rmd\#for-reviewers}{\texttt{latin-square}
--- For Reviewers}

\subsubsection{Left Truncation}\label{left-truncation}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Left Truncation} as implemented in \emph{{[}package vX.Y{]}} in
R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list checks
performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Left Truncation yielded \emph{{[}estimate{]}} (95\% CI \emph{{[}lower,
upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic / assumption
summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/11-survival/left-truncation.Rmd\#for-reviewers}{\texttt{left-truncation}
--- For Reviewers}

\subsubsection{Levene's Test of
Variances}\label{levenes-test-of-variances}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Levene's Test of Variances} as implemented in \emph{{[}package
vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list
checks performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Levene's Test of Variances yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/04-inferential/levene-test.Rmd\#for-reviewers}{\texttt{levene-test}
--- For Reviewers}

\subsubsection{Leverage and Influence}\label{leverage-and-influence}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Leverage and Influence} as implemented in \emph{{[}package
vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list
checks performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Leverage and Influence yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/07-regression/leverage-influence.Rmd\#for-reviewers}{\texttt{leverage-influence}
--- For Reviewers}

\subsubsection{Linear Regression
Assumptions}\label{linear-regression-assumptions}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Linear Regression Assumptions} as implemented in
\emph{{[}package vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were
checked: \emph{{[}list checks performed{]}}. \emph{{[}Effect size with
95\% confidence interval reported as the primary outcome{]}}. Two-sided
p-values use \emph{α = 0.05}; \emph{{[}multiple-comparison adjustment if
any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Linear Regression Assumptions yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/07-regression/regression-assumptions.Rmd\#for-reviewers}{\texttt{regression-assumptions}
--- For Reviewers}

\subsubsection{Log-Rank Test: Details}\label{log-rank-test-details}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Log-Rank Test: Details} as implemented in \emph{{[}package
vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list
checks performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Log-Rank Test: Details yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/11-survival/log-rank-test-details.Rmd\#for-reviewers}{\texttt{log-rank-test-details}
--- For Reviewers}

\subsubsection{Logistic Regression}\label{logistic-regression}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Logistic Regression} as implemented in \emph{{[}package vX.Y{]}}
in R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list checks
performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Logistic Regression yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/07-regression/logistic-regression.Rmd\#for-reviewers}{\texttt{logistic-regression}
--- For Reviewers}

\subsubsection{Logistic Regression
Diagnostics}\label{logistic-regression-diagnostics}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Logistic Regression Diagnostics} as implemented in
\emph{{[}package vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were
checked: \emph{{[}list checks performed{]}}. \emph{{[}Effect size with
95\% confidence interval reported as the primary outcome{]}}. Two-sided
p-values use \emph{α = 0.05}; \emph{{[}multiple-comparison adjustment if
any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Logistic Regression Diagnostics yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/07-regression/logistic-regression-diagnostics.Rmd\#for-reviewers}{\texttt{logistic-regression-diagnostics}
--- For Reviewers}

\subsubsection{Mann-Whitney U Test}\label{mann-whitney-u-test}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Mann-Whitney U Test} as implemented in \emph{{[}package vX.Y{]}}
in R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list checks
performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Mann-Whitney U Test yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/04-inferential/mann-whitney-u.Rmd\#for-reviewers}{\texttt{mann-whitney-u}
--- For Reviewers}

\subsubsection{McNemar's Test}\label{mcnemars-test}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{McNemar's Test} as implemented in \emph{{[}package vX.Y{]}} in R
\emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list checks
performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
McNemar's Test yielded \emph{{[}estimate{]}} (95\% CI \emph{{[}lower,
upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic / assumption
summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/04-inferential/mcnemar-test.Rmd\#for-reviewers}{\texttt{mcnemar-test}
--- For Reviewers}

\subsubsection{Method of Steepest
Ascent}\label{method-of-steepest-ascent}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Method of Steepest Ascent} as implemented in \emph{{[}package
vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list
checks performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Method of Steepest Ascent yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/16-design/steepest-ascent.Rmd\#for-reviewers}{\texttt{steepest-ascent}
--- For Reviewers}

\subsubsection{Minimum Detectable
Effect}\label{minimum-detectable-effect}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Minimum Detectable Effect} as implemented in \emph{{[}package
vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list
checks performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Minimum Detectable Effect yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/05-power/minimum-detectable-effect.Rmd\#for-reviewers}{\texttt{minimum-detectable-effect}
--- For Reviewers}

\subsubsection{Mixed ANOVA}\label{mixed-anova}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Mixed ANOVA} as implemented in \emph{{[}package vX.Y{]}} in R
\emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list checks
performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Mixed ANOVA yielded \emph{{[}estimate{]}} (95\% CI \emph{{[}lower,
upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic / assumption
summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/04-inferential/mixed-anova.Rmd\#for-reviewers}{\texttt{mixed-anova}
--- For Reviewers}

\subsubsection{Mixed-Effects Models:
Introduction}\label{mixed-effects-models-introduction}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Mixed-Effects Models: Introduction} as implemented in
\emph{{[}package vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were
checked: \emph{{[}list checks performed{]}}. \emph{{[}Effect size with
95\% confidence interval reported as the primary outcome{]}}. Two-sided
p-values use \emph{α = 0.05}; \emph{{[}multiple-comparison adjustment if
any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Mixed-Effects Models: Introduction yielded \emph{{[}estimate{]}} (95\%
CI \emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}).
\emph{{[}Diagnostic / assumption summary{]}}. \emph{{[}Sensitivity
analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/07-regression/mixed-models-intro.Rmd\#for-reviewers}{\texttt{mixed-models-intro}
--- For Reviewers}

\subsubsection{Model Selection with AIC and
BIC}\label{model-selection-with-aic-and-bic}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Model Selection with AIC and BIC} as implemented in
\emph{{[}package vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were
checked: \emph{{[}list checks performed{]}}. \emph{{[}Effect size with
95\% confidence interval reported as the primary outcome{]}}. Two-sided
p-values use \emph{α = 0.05}; \emph{{[}multiple-comparison adjustment if
any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Model Selection with AIC and BIC yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/07-regression/model-selection-aic-bic.Rmd\#for-reviewers}{\texttt{model-selection-aic-bic}
--- For Reviewers}

\subsubsection{Multi-State Models}\label{multi-state-models}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Multi-State Models} as implemented in \emph{{[}package vX.Y{]}}
in R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list checks
performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Multi-State Models yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/11-survival/multi-state-models.Rmd\#for-reviewers}{\texttt{multi-state-models}
--- For Reviewers}

\subsubsection{Multicollinearity and
VIF}\label{multicollinearity-and-vif}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Multicollinearity and VIF} as implemented in \emph{{[}package
vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list
checks performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Multicollinearity and VIF yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/07-regression/multicollinearity-vif.Rmd\#for-reviewers}{\texttt{multicollinearity-vif}
--- For Reviewers}

\subsubsection{Multinomial Logistic
Regression}\label{multinomial-logistic-regression}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Multinomial Logistic Regression} as implemented in
\emph{{[}package vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were
checked: \emph{{[}list checks performed{]}}. \emph{{[}Effect size with
95\% confidence interval reported as the primary outcome{]}}. Two-sided
p-values use \emph{α = 0.05}; \emph{{[}multiple-comparison adjustment if
any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Multinomial Logistic Regression yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/07-regression/multinomial-logistic.Rmd\#for-reviewers}{\texttt{multinomial-logistic}
--- For Reviewers}

\subsubsection{Multiple Comparisons:
Overview}\label{multiple-comparisons-overview}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Multiple Comparisons: Overview} as implemented in
\emph{{[}package vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were
checked: \emph{{[}list checks performed{]}}. \emph{{[}Effect size with
95\% confidence interval reported as the primary outcome{]}}. Two-sided
p-values use \emph{α = 0.05}; \emph{{[}multiple-comparison adjustment if
any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Multiple Comparisons: Overview yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/04-inferential/multiple-comparisons-overview.Rmd\#for-reviewers}{\texttt{multiple-comparisons-overview}
--- For Reviewers}

\subsubsection{Multiple Linear
Regression}\label{multiple-linear-regression}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Multiple Linear Regression} as implemented in \emph{{[}package
vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list
checks performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Multiple Linear Regression yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/07-regression/multiple-linear-regression.Rmd\#for-reviewers}{\texttt{multiple-linear-regression}
--- For Reviewers}

\subsubsection{Negative Binomial
Regression}\label{negative-binomial-regression}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Negative Binomial Regression} as implemented in \emph{{[}package
vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list
checks performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Negative Binomial Regression yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/07-regression/negative-binomial-regression.Rmd\#for-reviewers}{\texttt{negative-binomial-regression}
--- For Reviewers}

\subsubsection{Nested vs Crossed Random
Effects}\label{nested-vs-crossed-random-effects}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Nested vs Crossed Random Effects} as implemented in
\emph{{[}package vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were
checked: \emph{{[}list checks performed{]}}. \emph{{[}Effect size with
95\% confidence interval reported as the primary outcome{]}}. Two-sided
p-values use \emph{α = 0.05}; \emph{{[}multiple-comparison adjustment if
any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Nested vs Crossed Random Effects yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/07-regression/nested-crossed-random-effects.Rmd\#for-reviewers}{\texttt{nested-crossed-random-effects}
--- For Reviewers}

\subsubsection{Null and Alternative
Hypotheses}\label{null-and-alternative-hypotheses}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Null and Alternative Hypotheses} as implemented in
\emph{{[}package vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were
checked: \emph{{[}list checks performed{]}}. \emph{{[}Effect size with
95\% confidence interval reported as the primary outcome{]}}. Two-sided
p-values use \emph{α = 0.05}; \emph{{[}multiple-comparison adjustment if
any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Null and Alternative Hypotheses yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/04-inferential/null-alternative-hypotheses.Rmd\#for-reviewers}{\texttt{null-alternative-hypotheses}
--- For Reviewers}

\subsubsection{Offsets in Regression}\label{offsets-in-regression}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Offsets in Regression} as implemented in \emph{{[}package
vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list
checks performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Offsets in Regression yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/07-regression/offsets-in-regression.Rmd\#for-reviewers}{\texttt{offsets-in-regression}
--- For Reviewers}

\subsubsection{One-Proportion Test}\label{one-proportion-test}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{One-Proportion Test} as implemented in \emph{{[}package vX.Y{]}}
in R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list checks
performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
One-Proportion Test yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/04-inferential/one-proportion-test.Rmd\#for-reviewers}{\texttt{one-proportion-test}
--- For Reviewers}

\subsubsection{One-Sample t-Test}\label{one-sample-t-test}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{One-Sample t-Test} as implemented in \emph{{[}package vX.Y{]}}
in R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list checks
performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
One-Sample t-Test yielded \emph{{[}estimate{]}} (95\% CI \emph{{[}lower,
upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic / assumption
summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/04-inferential/one-sample-t-test.Rmd\#for-reviewers}{\texttt{one-sample-t-test}
--- For Reviewers}

\subsubsection{One-Sample z-Test}\label{one-sample-z-test}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{One-Sample z-Test} as implemented in \emph{{[}package vX.Y{]}}
in R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list checks
performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
One-Sample z-Test yielded \emph{{[}estimate{]}} (95\% CI \emph{{[}lower,
upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic / assumption
summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/04-inferential/one-sample-z-test.Rmd\#for-reviewers}{\texttt{one-sample-z-test}
--- For Reviewers}

\subsubsection{One-Way ANOVA}\label{one-way-anova}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{One-Way ANOVA} as implemented in \emph{{[}package vX.Y{]}} in R
\emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list checks
performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
One-Way ANOVA yielded \emph{{[}estimate{]}} (95\% CI \emph{{[}lower,
upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic / assumption
summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/04-inferential/one-way-anova.Rmd\#for-reviewers}{\texttt{one-way-anova}
--- For Reviewers}

\subsubsection{Optimal Designs: D, A, I}\label{optimal-designs-d-a-i}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Optimal Designs: D, A, I} as implemented in \emph{{[}package
vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list
checks performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Optimal Designs: D, A, I yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/16-design/optimal-design-d-a-i.Rmd\#for-reviewers}{\texttt{optimal-design-d-a-i}
--- For Reviewers}

\subsubsection{Ordinal Logistic
Regression}\label{ordinal-logistic-regression}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Ordinal Logistic Regression} as implemented in \emph{{[}package
vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list
checks performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Ordinal Logistic Regression yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/07-regression/ordinal-logistic.Rmd\#for-reviewers}{\texttt{ordinal-logistic}
--- For Reviewers}

\subsubsection{Orthogonal Arrays}\label{orthogonal-arrays}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Orthogonal Arrays} as implemented in \emph{{[}package vX.Y{]}}
in R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list checks
performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Orthogonal Arrays yielded \emph{{[}estimate{]}} (95\% CI \emph{{[}lower,
upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic / assumption
summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/16-design/orthogonal-arrays.Rmd\#for-reviewers}{\texttt{orthogonal-arrays}
--- For Reviewers}

\subsubsection{P-Values Explained}\label{p-values-explained}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{P-Values Explained} as implemented in \emph{{[}package vX.Y{]}}
in R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list checks
performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
P-Values Explained yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/04-inferential/p-values-explained.Rmd\#for-reviewers}{\texttt{p-values-explained}
--- For Reviewers}

\subsubsection{Paired t-Test}\label{paired-t-test}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Paired t-Test} as implemented in \emph{{[}package vX.Y{]}} in R
\emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list checks
performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Paired t-Test yielded \emph{{[}estimate{]}} (95\% CI \emph{{[}lower,
upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic / assumption
summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/04-inferential/paired-t-test.Rmd\#for-reviewers}{\texttt{paired-t-test}
--- For Reviewers}

\subsubsection{Parametric Survival:
Log-Normal}\label{parametric-survival-log-normal}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Parametric Survival: Log-Normal} as implemented in
\emph{{[}package vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were
checked: \emph{{[}list checks performed{]}}. \emph{{[}Effect size with
95\% confidence interval reported as the primary outcome{]}}. Two-sided
p-values use \emph{α = 0.05}; \emph{{[}multiple-comparison adjustment if
any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Parametric Survival: Log-Normal yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/11-survival/parametric-survival-lognormal.Rmd\#for-reviewers}{\texttt{parametric-survival-lognormal}
--- For Reviewers}

\subsubsection{Parametric Survival:
Weibull}\label{parametric-survival-weibull}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Parametric Survival: Weibull} as implemented in \emph{{[}package
vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list
checks performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Parametric Survival: Weibull yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/11-survival/parametric-survival-weibull.Rmd\#for-reviewers}{\texttt{parametric-survival-weibull}
--- For Reviewers}

\subsubsection{Partial Correlation}\label{partial-correlation}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Partial Correlation} as implemented in \emph{{[}package vX.Y{]}}
in R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list checks
performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Partial Correlation yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/07-regression/partial-correlation.Rmd\#for-reviewers}{\texttt{partial-correlation}
--- For Reviewers}

\subsubsection{Pearson Correlation Test}\label{pearson-correlation-test}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Pearson Correlation Test} as implemented in \emph{{[}package
vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list
checks performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Pearson Correlation Test yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/04-inferential/pearson-correlation-test.Rmd\#for-reviewers}{\texttt{pearson-correlation-test}
--- For Reviewers}

\subsubsection{Permutation Tests}\label{permutation-tests}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Permutation Tests} as implemented in \emph{{[}package vX.Y{]}}
in R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list checks
performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Permutation Tests yielded \emph{{[}estimate{]}} (95\% CI \emph{{[}lower,
upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic / assumption
summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/04-inferential/permutation-tests.Rmd\#for-reviewers}{\texttt{permutation-tests}
--- For Reviewers}

\subsubsection{Plackett-Burman Designs}\label{plackett-burman-designs}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Plackett-Burman Designs} as implemented in \emph{{[}package
vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list
checks performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Plackett-Burman Designs yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/16-design/plackett-burman.Rmd\#for-reviewers}{\texttt{plackett-burman}
--- For Reviewers}

\subsubsection{Poisson Regression}\label{poisson-regression}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Poisson Regression} as implemented in \emph{{[}package vX.Y{]}}
in R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list checks
performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Poisson Regression yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/07-regression/poisson-regression.Rmd\#for-reviewers}{\texttt{poisson-regression}
--- For Reviewers}

\subsubsection{Polynomial Regression}\label{polynomial-regression}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Polynomial Regression} as implemented in \emph{{[}package
vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list
checks performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Polynomial Regression yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/07-regression/polynomial-regression.Rmd\#for-reviewers}{\texttt{polynomial-regression}
--- For Reviewers}

\subsubsection{Post-Hoc Power: A
Controversy}\label{post-hoc-power-a-controversy}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Post-Hoc Power: A Controversy} as implemented in
\emph{{[}package vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were
checked: \emph{{[}list checks performed{]}}. \emph{{[}Effect size with
95\% confidence interval reported as the primary outcome{]}}. Two-sided
p-values use \emph{α = 0.05}; \emph{{[}multiple-comparison adjustment if
any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Post-Hoc Power: A Controversy yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/05-power/post-hoc-power-controversy.Rmd\#for-reviewers}{\texttt{post-hoc-power-controversy}
--- For Reviewers}

\subsubsection{Post-Hoc Tests with Tukey
HSD}\label{post-hoc-tests-with-tukey-hsd}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Post-Hoc Tests with Tukey HSD} as implemented in
\emph{{[}package vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were
checked: \emph{{[}list checks performed{]}}. \emph{{[}Effect size with
95\% confidence interval reported as the primary outcome{]}}. Two-sided
p-values use \emph{α = 0.05}; \emph{{[}multiple-comparison adjustment if
any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Post-Hoc Tests with Tukey HSD yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/04-inferential/post-hoc-tests-tukey.Rmd\#for-reviewers}{\texttt{post-hoc-tests-tukey}
--- For Reviewers}

\subsubsection{Power Analysis for DOE}\label{power-analysis-for-doe}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Power Analysis for DOE} as implemented in \emph{{[}package
vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list
checks performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Power Analysis for DOE yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/16-design/power-in-doe.Rmd\#for-reviewers}{\texttt{power-in-doe}
--- For Reviewers}

\subsubsection{Power Analysis:
Introduction}\label{power-analysis-introduction}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Power Analysis: Introduction} as implemented in \emph{{[}package
vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list
checks performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Power Analysis: Introduction yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/05-power/power-analysis-introduction.Rmd\#for-reviewers}{\texttt{power-analysis-introduction}
--- For Reviewers}

\subsubsection{Power for Agreement
(Kappa)}\label{power-for-agreement-kappa}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Power for Agreement (Kappa)} as implemented in \emph{{[}package
vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list
checks performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Power for Agreement (Kappa) yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/05-power/power-agreement-kappa.Rmd\#for-reviewers}{\texttt{power-agreement-kappa}
--- For Reviewers}

\subsubsection{Power for Bland-Altman
Studies}\label{power-for-bland-altman-studies}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Power for Bland-Altman Studies} as implemented in
\emph{{[}package vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were
checked: \emph{{[}list checks performed{]}}. \emph{{[}Effect size with
95\% confidence interval reported as the primary outcome{]}}. Two-sided
p-values use \emph{α = 0.05}; \emph{{[}multiple-comparison adjustment if
any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Power for Bland-Altman Studies yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/05-power/power-bland-altman.Rmd\#for-reviewers}{\texttt{power-bland-altman}
--- For Reviewers}

\subsubsection{Power for Chi-Squared
Tests}\label{power-for-chi-squared-tests}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Power for Chi-Squared Tests} as implemented in \emph{{[}package
vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list
checks performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Power for Chi-Squared Tests yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/05-power/power-chi-squared.Rmd\#for-reviewers}{\texttt{power-chi-squared}
--- For Reviewers}

\subsubsection{Power for Cluster-RCT}\label{power-for-cluster-rct}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Power for Cluster-RCT} as implemented in \emph{{[}package
vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list
checks performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Power for Cluster-RCT yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/05-power/power-cluster-rct.Rmd\#for-reviewers}{\texttt{power-cluster-rct}
--- For Reviewers}

\subsubsection{Power for Correlation
Tests}\label{power-for-correlation-tests}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Power for Correlation Tests} as implemented in \emph{{[}package
vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list
checks performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Power for Correlation Tests yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/05-power/power-correlation.Rmd\#for-reviewers}{\texttt{power-correlation}
--- For Reviewers}

\subsubsection{Power for Cox Regression}\label{power-for-cox-regression}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Power for Cox Regression} as implemented in \emph{{[}package
vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list
checks performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Power for Cox Regression yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/05-power/power-cox-regression.Rmd\#for-reviewers}{\texttt{power-cox-regression}
--- For Reviewers}

\subsubsection{Power for Crossover
Trials}\label{power-for-crossover-trials}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Power for Crossover Trials} as implemented in \emph{{[}package
vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list
checks performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Power for Crossover Trials yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/05-power/power-crossover.Rmd\#for-reviewers}{\texttt{power-crossover}
--- For Reviewers}

\subsubsection{Power for Diagnostic
Accuracy}\label{power-for-diagnostic-accuracy}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Power for Diagnostic Accuracy} as implemented in
\emph{{[}package vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were
checked: \emph{{[}list checks performed{]}}. \emph{{[}Effect size with
95\% confidence interval reported as the primary outcome{]}}. Two-sided
p-values use \emph{α = 0.05}; \emph{{[}multiple-comparison adjustment if
any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Power for Diagnostic Accuracy yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/05-power/power-diagnostic-accuracy.Rmd\#for-reviewers}{\texttt{power-diagnostic-accuracy}
--- For Reviewers}

\subsubsection{Power for Equivalence
(TOST)}\label{power-for-equivalence-tost}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Power for Equivalence (TOST)} as implemented in \emph{{[}package
vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list
checks performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Power for Equivalence (TOST) yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/05-power/power-equivalence-tost.Rmd\#for-reviewers}{\texttt{power-equivalence-tost}
--- For Reviewers}

\subsubsection{Power for ICC}\label{power-for-icc}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Power for ICC} as implemented in \emph{{[}package vX.Y{]}} in R
\emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list checks
performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Power for ICC yielded \emph{{[}estimate{]}} (95\% CI \emph{{[}lower,
upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic / assumption
summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/05-power/power-icc.Rmd\#for-reviewers}{\texttt{power-icc}
--- For Reviewers}

\subsubsection{Power for Linear
Regression}\label{power-for-linear-regression}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Power for Linear Regression} as implemented in \emph{{[}package
vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list
checks performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Power for Linear Regression yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/05-power/power-linear-regression.Rmd\#for-reviewers}{\texttt{power-linear-regression}
--- For Reviewers}

\subsubsection{Power for Logistic
Regression}\label{power-for-logistic-regression}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Power for Logistic Regression} as implemented in
\emph{{[}package vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were
checked: \emph{{[}list checks performed{]}}. \emph{{[}Effect size with
95\% confidence interval reported as the primary outcome{]}}. Two-sided
p-values use \emph{α = 0.05}; \emph{{[}multiple-comparison adjustment if
any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Power for Logistic Regression yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/05-power/power-logistic-regression.Rmd\#for-reviewers}{\texttt{power-logistic-regression}
--- For Reviewers}

\subsubsection{Power for McNemar's Test}\label{power-for-mcnemars-test}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Power for McNemar's Test} as implemented in \emph{{[}package
vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list
checks performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Power for McNemar's Test yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/05-power/power-mcnemar.Rmd\#for-reviewers}{\texttt{power-mcnemar}
--- For Reviewers}

\subsubsection{Power for Non-Inferiority
Trials}\label{power-for-non-inferiority-trials}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Power for Non-Inferiority Trials} as implemented in
\emph{{[}package vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were
checked: \emph{{[}list checks performed{]}}. \emph{{[}Effect size with
95\% confidence interval reported as the primary outcome{]}}. Two-sided
p-values use \emph{α = 0.05}; \emph{{[}multiple-comparison adjustment if
any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Power for Non-Inferiority Trials yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/05-power/power-non-inferiority.Rmd\#for-reviewers}{\texttt{power-non-inferiority}
--- For Reviewers}

\subsubsection{Power for One-Proportion
Test}\label{power-for-one-proportion-test}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Power for One-Proportion Test} as implemented in
\emph{{[}package vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were
checked: \emph{{[}list checks performed{]}}. \emph{{[}Effect size with
95\% confidence interval reported as the primary outcome{]}}. Two-sided
p-values use \emph{α = 0.05}; \emph{{[}multiple-comparison adjustment if
any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Power for One-Proportion Test yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/05-power/power-one-proportion.Rmd\#for-reviewers}{\texttt{power-one-proportion}
--- For Reviewers}

\subsubsection{Power for One-Sample
t-Test}\label{power-for-one-sample-t-test}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Power for One-Sample t-Test} as implemented in \emph{{[}package
vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list
checks performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Power for One-Sample t-Test yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/05-power/power-one-sample-t.Rmd\#for-reviewers}{\texttt{power-one-sample-t}
--- For Reviewers}

\subsubsection{Power for One-Way ANOVA}\label{power-for-one-way-anova}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Power for One-Way ANOVA} as implemented in \emph{{[}package
vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list
checks performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Power for One-Way ANOVA yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/05-power/power-anova.Rmd\#for-reviewers}{\texttt{power-anova}
--- For Reviewers}

\subsubsection{Power for Paired t-Test}\label{power-for-paired-t-test}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Power for Paired t-Test} as implemented in \emph{{[}package
vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list
checks performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Power for Paired t-Test yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/05-power/power-paired-t.Rmd\#for-reviewers}{\texttt{power-paired-t}
--- For Reviewers}

\subsubsection{Power for Repeated-Measures
ANOVA}\label{power-for-repeated-measures-anova}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Power for Repeated-Measures ANOVA} as implemented in
\emph{{[}package vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were
checked: \emph{{[}list checks performed{]}}. \emph{{[}Effect size with
95\% confidence interval reported as the primary outcome{]}}. Two-sided
p-values use \emph{α = 0.05}; \emph{{[}multiple-comparison adjustment if
any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Power for Repeated-Measures ANOVA yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/05-power/power-repeated-measures.Rmd\#for-reviewers}{\texttt{power-repeated-measures}
--- For Reviewers}

\subsubsection{Power for Stepped-Wedge
Trials}\label{power-for-stepped-wedge-trials}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Power for Stepped-Wedge Trials} as implemented in
\emph{{[}package vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were
checked: \emph{{[}list checks performed{]}}. \emph{{[}Effect size with
95\% confidence interval reported as the primary outcome{]}}. Two-sided
p-values use \emph{α = 0.05}; \emph{{[}multiple-comparison adjustment if
any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Power for Stepped-Wedge Trials yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/05-power/power-stepped-wedge.Rmd\#for-reviewers}{\texttt{power-stepped-wedge}
--- For Reviewers}

\subsubsection{Power for the Log-Rank
Test}\label{power-for-the-log-rank-test}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Power for the Log-Rank Test} as implemented in \emph{{[}package
vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list
checks performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Power for the Log-Rank Test yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/05-power/power-logrank-test.Rmd\#for-reviewers}{\texttt{power-logrank-test}
--- For Reviewers}

\subsubsection{Power for Two-Proportion
Test}\label{power-for-two-proportion-test}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Power for Two-Proportion Test} as implemented in
\emph{{[}package vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were
checked: \emph{{[}list checks performed{]}}. \emph{{[}Effect size with
95\% confidence interval reported as the primary outcome{]}}. Two-sided
p-values use \emph{α = 0.05}; \emph{{[}multiple-comparison adjustment if
any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Power for Two-Proportion Test yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/05-power/power-two-proportions.Rmd\#for-reviewers}{\texttt{power-two-proportions}
--- For Reviewers}

\subsubsection{Principles of Blocking}\label{principles-of-blocking}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Principles of Blocking} as implemented in \emph{{[}package
vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list
checks performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Principles of Blocking yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/16-design/blocking-principles.Rmd\#for-reviewers}{\texttt{blocking-principles}
--- For Reviewers}

\subsubsection{Probit Regression}\label{probit-regression}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Probit Regression} as implemented in \emph{{[}package vX.Y{]}}
in R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list checks
performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Probit Regression yielded \emph{{[}estimate{]}} (95\% CI \emph{{[}lower,
upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic / assumption
summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/07-regression/probit-regression.Rmd\#for-reviewers}{\texttt{probit-regression}
--- For Reviewers}

\subsubsection{Quantile Regression}\label{quantile-regression}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Quantile Regression} as implemented in \emph{{[}package vX.Y{]}}
in R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list checks
performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Quantile Regression yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/07-regression/quantile-regression.Rmd\#for-reviewers}{\texttt{quantile-regression}
--- For Reviewers}

\subsubsection{Quasi-Poisson Regression}\label{quasi-poisson-regression}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Quasi-Poisson Regression} as implemented in \emph{{[}package
vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list
checks performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Quasi-Poisson Regression yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/07-regression/quasi-poisson.Rmd\#for-reviewers}{\texttt{quasi-poisson}
--- For Reviewers}

\subsubsection{R-Squared and Adjusted
R-Squared}\label{r-squared-and-adjusted-r-squared}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{R-Squared and Adjusted R-Squared} as implemented in
\emph{{[}package vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were
checked: \emph{{[}list checks performed{]}}. \emph{{[}Effect size with
95\% confidence interval reported as the primary outcome{]}}. Two-sided
p-values use \emph{α = 0.05}; \emph{{[}multiple-comparison adjustment if
any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
R-Squared and Adjusted R-Squared yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/07-regression/r-squared-adjusted.Rmd\#for-reviewers}{\texttt{r-squared-adjusted}
--- For Reviewers}

\subsubsection{Random-Intercept Models}\label{random-intercept-models}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Random-Intercept Models} as implemented in \emph{{[}package
vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list
checks performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Random-Intercept Models yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/07-regression/random-intercepts.Rmd\#for-reviewers}{\texttt{random-intercepts}
--- For Reviewers}

\subsubsection{Random-Slope Models}\label{random-slope-models}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Random-Slope Models} as implemented in \emph{{[}package vX.Y{]}}
in R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list checks
performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Random-Slope Models yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/07-regression/random-slopes.Rmd\#for-reviewers}{\texttt{random-slopes}
--- For Reviewers}

\subsubsection{Randomisation in Design}\label{randomisation-in-design}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Randomisation in Design} as implemented in \emph{{[}package
vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list
checks performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Randomisation in Design yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/16-design/randomization-in-design.Rmd\#for-reviewers}{\texttt{randomization-in-design}
--- For Reviewers}

\subsubsection{Randomised Complete Block
Design}\label{randomised-complete-block-design}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Randomised Complete Block Design} as implemented in
\emph{{[}package vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were
checked: \emph{{[}list checks performed{]}}. \emph{{[}Effect size with
95\% confidence interval reported as the primary outcome{]}}. Two-sided
p-values use \emph{α = 0.05}; \emph{{[}multiple-comparison adjustment if
any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Randomised Complete Block Design yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/16-design/randomised-complete-block.Rmd\#for-reviewers}{\texttt{randomised-complete-block}
--- For Reviewers}

\subsubsection{Recurrent Events}\label{recurrent-events}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Recurrent Events} as implemented in \emph{{[}package vX.Y{]}} in
R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list checks
performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Recurrent Events yielded \emph{{[}estimate{]}} (95\% CI \emph{{[}lower,
upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic / assumption
summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/11-survival/recurrent-events.Rmd\#for-reviewers}{\texttt{recurrent-events}
--- For Reviewers}

\subsubsection{Repeated-Measures ANOVA}\label{repeated-measures-anova}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Repeated-Measures ANOVA} as implemented in \emph{{[}package
vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list
checks performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Repeated-Measures ANOVA yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/04-inferential/repeated-measures-anova.Rmd\#for-reviewers}{\texttt{repeated-measures-anova}
--- For Reviewers}

\subsubsection{Repeated-Measures
Designs}\label{repeated-measures-designs}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Repeated-Measures Designs} as implemented in \emph{{[}package
vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list
checks performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Repeated-Measures Designs yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/16-design/repeated-measures-design.Rmd\#for-reviewers}{\texttt{repeated-measures-design}
--- For Reviewers}

\subsubsection{Residual Diagnostics}\label{residual-diagnostics}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Residual Diagnostics} as implemented in \emph{{[}package
vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list
checks performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Residual Diagnostics yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/07-regression/residual-diagnostics.Rmd\#for-reviewers}{\texttt{residual-diagnostics}
--- For Reviewers}

\subsubsection{Resolution and Aliasing}\label{resolution-and-aliasing}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Resolution and Aliasing} as implemented in \emph{{[}package
vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list
checks performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Resolution and Aliasing yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/16-design/resolution-aliasing.Rmd\#for-reviewers}{\texttt{resolution-aliasing}
--- For Reviewers}

\subsubsection{Response Surface
Methodology}\label{response-surface-methodology}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Response Surface Methodology} as implemented in \emph{{[}package
vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list
checks performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Response Surface Methodology yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/16-design/response-surface-methodology.Rmd\#for-reviewers}{\texttt{response-surface-methodology}
--- For Reviewers}

\subsubsection{Restricted Mean Survival Time
(RMST)}\label{restricted-mean-survival-time-rmst}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Restricted Mean Survival Time (RMST)} as implemented in
\emph{{[}package vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were
checked: \emph{{[}list checks performed{]}}. \emph{{[}Effect size with
95\% confidence interval reported as the primary outcome{]}}. Two-sided
p-values use \emph{α = 0.05}; \emph{{[}multiple-comparison adjustment if
any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Restricted Mean Survival Time (RMST) yielded \emph{{[}estimate{]}} (95\%
CI \emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}).
\emph{{[}Diagnostic / assumption summary{]}}. \emph{{[}Sensitivity
analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/11-survival/restricted-mean-survival-time.Rmd\#for-reviewers}{\texttt{restricted-mean-survival-time}
--- For Reviewers}

\subsubsection{Ridge Regression}\label{ridge-regression}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Ridge Regression} as implemented in \emph{{[}package vX.Y{]}} in
R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list checks
performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Ridge Regression yielded \emph{{[}estimate{]}} (95\% CI \emph{{[}lower,
upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic / assumption
summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/07-regression/ridge-regression.Rmd\#for-reviewers}{\texttt{ridge-regression}
--- For Reviewers}

\subsubsection{Robust Parameter Design}\label{robust-parameter-design}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Robust Parameter Design} as implemented in \emph{{[}package
vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list
checks performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Robust Parameter Design yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/16-design/robust-parameter-design.Rmd\#for-reviewers}{\texttt{robust-parameter-design}
--- For Reviewers}

\subsubsection{Robust Regression}\label{robust-regression}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Robust Regression} as implemented in \emph{{[}package vX.Y{]}}
in R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list checks
performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Robust Regression yielded \emph{{[}estimate{]}} (95\% CI \emph{{[}lower,
upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic / assumption
summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/07-regression/robust-regression.Rmd\#for-reviewers}{\texttt{robust-regression}
--- For Reviewers}

\subsubsection{ROC and AUC for Logistic
Models}\label{roc-and-auc-for-logistic-models}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{ROC and AUC for Logistic Models} as implemented in
\emph{{[}package vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were
checked: \emph{{[}list checks performed{]}}. \emph{{[}Effect size with
95\% confidence interval reported as the primary outcome{]}}. Two-sided
p-values use \emph{α = 0.05}; \emph{{[}multiple-comparison adjustment if
any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
ROC and AUC for Logistic Models yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/07-regression/roc-auc-for-logistic.Rmd\#for-reviewers}{\texttt{roc-auc-for-logistic}
--- For Reviewers}

\subsubsection{Royston-Parmar Flexible Parametric
Models}\label{royston-parmar-flexible-parametric-models}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Royston-Parmar Flexible Parametric Models} as implemented in
\emph{{[}package vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were
checked: \emph{{[}list checks performed{]}}. \emph{{[}Effect size with
95\% confidence interval reported as the primary outcome{]}}. Two-sided
p-values use \emph{α = 0.05}; \emph{{[}multiple-comparison adjustment if
any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Royston-Parmar Flexible Parametric Models yielded \emph{{[}estimate{]}}
(95\% CI \emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}).
\emph{{[}Diagnostic / assumption summary{]}}. \emph{{[}Sensitivity
analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/11-survival/flexible-parametric-royston-parmar.Rmd\#for-reviewers}{\texttt{flexible-parametric-royston-parmar}
--- For Reviewers}

\subsubsection{Sample Size for a Two-Sample
t-Test}\label{sample-size-for-a-two-sample-t-test}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Sample Size for a Two-Sample t-Test} as implemented in
\emph{{[}package vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were
checked: \emph{{[}list checks performed{]}}. \emph{{[}Effect size with
95\% confidence interval reported as the primary outcome{]}}. Two-sided
p-values use \emph{α = 0.05}; \emph{{[}multiple-comparison adjustment if
any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Sample Size for a Two-Sample t-Test yielded \emph{{[}estimate{]}} (95\%
CI \emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}).
\emph{{[}Diagnostic / assumption summary{]}}. \emph{{[}Sensitivity
analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/05-power/power-two-sample-t.Rmd\#for-reviewers}{\texttt{power-two-sample-t}
--- For Reviewers}

\subsubsection{Sample Size Sensitivity
Analysis}\label{sample-size-sensitivity-analysis}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Sample Size Sensitivity Analysis} as implemented in
\emph{{[}package vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were
checked: \emph{{[}list checks performed{]}}. \emph{{[}Effect size with
95\% confidence interval reported as the primary outcome{]}}. Two-sided
p-values use \emph{α = 0.05}; \emph{{[}multiple-comparison adjustment if
any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Sample Size Sensitivity Analysis yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/05-power/sensitivity-analysis-sample-size.Rmd\#for-reviewers}{\texttt{sensitivity-analysis-sample-size}
--- For Reviewers}

\subsubsection{Schoenfeld Residuals}\label{schoenfeld-residuals}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Schoenfeld Residuals} as implemented in \emph{{[}package
vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list
checks performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Schoenfeld Residuals yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/11-survival/schoenfeld-residuals.Rmd\#for-reviewers}{\texttt{schoenfeld-residuals}
--- For Reviewers}

\subsubsection{Shapiro-Wilk Normality
Test}\label{shapiro-wilk-normality-test}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Shapiro-Wilk Normality Test} as implemented in \emph{{[}package
vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list
checks performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Shapiro-Wilk Normality Test yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/04-inferential/shapiro-wilk-test.Rmd\#for-reviewers}{\texttt{shapiro-wilk-test}
--- For Reviewers}

\subsubsection{Signal-to-Noise Ratios}\label{signal-to-noise-ratios}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Signal-to-Noise Ratios} as implemented in \emph{{[}package
vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list
checks performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Signal-to-Noise Ratios yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/16-design/signal-to-noise-ratio.Rmd\#for-reviewers}{\texttt{signal-to-noise-ratio}
--- For Reviewers}

\subsubsection{Simple Linear Regression}\label{simple-linear-regression}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Simple Linear Regression} as implemented in \emph{{[}package
vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list
checks performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Simple Linear Regression yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/07-regression/simple-linear-regression.Rmd\#for-reviewers}{\texttt{simple-linear-regression}
--- For Reviewers}

\subsubsection{Simplex Centroid Designs}\label{simplex-centroid-designs}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Simplex Centroid Designs} as implemented in \emph{{[}package
vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list
checks performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Simplex Centroid Designs yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/16-design/mixture-centroid.Rmd\#for-reviewers}{\texttt{mixture-centroid}
--- For Reviewers}

\subsubsection{Simplex Lattice Mixture
Designs}\label{simplex-lattice-mixture-designs}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Simplex Lattice Mixture Designs} as implemented in
\emph{{[}package vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were
checked: \emph{{[}list checks performed{]}}. \emph{{[}Effect size with
95\% confidence interval reported as the primary outcome{]}}. Two-sided
p-values use \emph{α = 0.05}; \emph{{[}multiple-comparison adjustment if
any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Simplex Lattice Mixture Designs yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/16-design/mixture-designs-simplex.Rmd\#for-reviewers}{\texttt{mixture-designs-simplex}
--- For Reviewers}

\subsubsection{Simulating Survival Data}\label{simulating-survival-data}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Simulating Survival Data} as implemented in \emph{{[}package
vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list
checks performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Simulating Survival Data yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/11-survival/survival-simulation.Rmd\#for-reviewers}{\texttt{survival-simulation}
--- For Reviewers}

\subsubsection{Spearman Rank Correlation
Test}\label{spearman-rank-correlation-test}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Spearman Rank Correlation Test} as implemented in
\emph{{[}package vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were
checked: \emph{{[}list checks performed{]}}. \emph{{[}Effect size with
95\% confidence interval reported as the primary outcome{]}}. Two-sided
p-values use \emph{α = 0.05}; \emph{{[}multiple-comparison adjustment if
any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Spearman Rank Correlation Test yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/04-inferential/spearman-correlation-test.Rmd\#for-reviewers}{\texttt{spearman-correlation-test}
--- For Reviewers}

\subsubsection{Spline Regression}\label{spline-regression}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Spline Regression} as implemented in \emph{{[}package vX.Y{]}}
in R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list checks
performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Spline Regression yielded \emph{{[}estimate{]}} (95\% CI \emph{{[}lower,
upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic / assumption
summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/07-regression/spline-regression.Rmd\#for-reviewers}{\texttt{spline-regression}
--- For Reviewers}

\subsubsection{Split-Plot Designs}\label{split-plot-designs}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Split-Plot Designs} as implemented in \emph{{[}package vX.Y{]}}
in R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list checks
performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Split-Plot Designs yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/16-design/split-plot-design.Rmd\#for-reviewers}{\texttt{split-plot-design}
--- For Reviewers}

\subsubsection{Stepwise Regression:
Pitfalls}\label{stepwise-regression-pitfalls}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Stepwise Regression: Pitfalls} as implemented in
\emph{{[}package vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were
checked: \emph{{[}list checks performed{]}}. \emph{{[}Effect size with
95\% confidence interval reported as the primary outcome{]}}. Two-sided
p-values use \emph{α = 0.05}; \emph{{[}multiple-comparison adjustment if
any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Stepwise Regression: Pitfalls yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/07-regression/stepwise-regression-pitfalls.Rmd\#for-reviewers}{\texttt{stepwise-regression-pitfalls}
--- For Reviewers}

\subsubsection{Stratified Cox Models}\label{stratified-cox-models}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Stratified Cox Models} as implemented in \emph{{[}package
vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list
checks performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Stratified Cox Models yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/11-survival/stratified-cox.Rmd\#for-reviewers}{\texttt{stratified-cox}
--- For Reviewers}

\subsubsection{Strip-Plot Designs}\label{strip-plot-designs}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Strip-Plot Designs} as implemented in \emph{{[}package vX.Y{]}}
in R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list checks
performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Strip-Plot Designs yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/16-design/strip-plot-design.Rmd\#for-reviewers}{\texttt{strip-plot-design}
--- For Reviewers}

\subsubsection{Taguchi Methods}\label{taguchi-methods}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Taguchi Methods} as implemented in \emph{{[}package vX.Y{]}} in
R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list checks
performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Taguchi Methods yielded \emph{{[}estimate{]}} (95\% CI \emph{{[}lower,
upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic / assumption
summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/16-design/taguchi-methods.Rmd\#for-reviewers}{\texttt{taguchi-methods}
--- For Reviewers}

\subsubsection{The Bootstrap:
Introduction}\label{the-bootstrap-introduction}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{The Bootstrap: Introduction} as implemented in \emph{{[}package
vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list
checks performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
The Bootstrap: Introduction yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/04-inferential/bootstrap-introduction.Rmd\#for-reviewers}{\texttt{bootstrap-introduction}
--- For Reviewers}

\subsubsection{The Brier Score for
Survival}\label{the-brier-score-for-survival}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{The Brier Score for Survival} as implemented in \emph{{[}package
vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list
checks performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
The Brier Score for Survival yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/11-survival/brier-score-survival.Rmd\#for-reviewers}{\texttt{brier-score-survival}
--- For Reviewers}

\subsubsection{The Jackknife}\label{the-jackknife}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{The Jackknife} as implemented in \emph{{[}package vX.Y{]}} in R
\emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list checks
performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
The Jackknife yielded \emph{{[}estimate{]}} (95\% CI \emph{{[}lower,
upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic / assumption
summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/04-inferential/jackknife.Rmd\#for-reviewers}{\texttt{jackknife}
--- For Reviewers}

\subsubsection{The Nelson-Aalen
Estimator}\label{the-nelson-aalen-estimator}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{The Nelson-Aalen Estimator} as implemented in \emph{{[}package
vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list
checks performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
The Nelson-Aalen Estimator yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/11-survival/nelson-aalen-estimator.Rmd\#for-reviewers}{\texttt{nelson-aalen-estimator}
--- For Reviewers}

\subsubsection{The Runs Test}\label{the-runs-test}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{The Runs Test} as implemented in \emph{{[}package vX.Y{]}} in R
\emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list checks
performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
The Runs Test yielded \emph{{[}estimate{]}} (95\% CI \emph{{[}lower,
upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic / assumption
summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/04-inferential/runs-test.Rmd\#for-reviewers}{\texttt{runs-test}
--- For Reviewers}

\subsubsection{The Sign Test}\label{the-sign-test}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{The Sign Test} as implemented in \emph{{[}package vX.Y{]}} in R
\emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list checks
performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
The Sign Test yielded \emph{{[}estimate{]}} (95\% CI \emph{{[}lower,
upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic / assumption
summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/04-inferential/sign-test.Rmd\#for-reviewers}{\texttt{sign-test}
--- For Reviewers}

\subsubsection{Time-Dependent ROC}\label{time-dependent-roc}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Time-Dependent ROC} as implemented in \emph{{[}package vX.Y{]}}
in R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list checks
performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Time-Dependent ROC yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/11-survival/time-dependent-roc.Rmd\#for-reviewers}{\texttt{time-dependent-roc}
--- For Reviewers}

\subsubsection{Time-Varying Covariates}\label{time-varying-covariates}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Time-Varying Covariates} as implemented in \emph{{[}package
vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list
checks performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Time-Varying Covariates yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/11-survival/time-varying-covariates.Rmd\#for-reviewers}{\texttt{time-varying-covariates}
--- For Reviewers}

\subsubsection{Tobit Regression}\label{tobit-regression}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Tobit Regression} as implemented in \emph{{[}package vX.Y{]}} in
R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list checks
performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Tobit Regression yielded \emph{{[}estimate{]}} (95\% CI \emph{{[}lower,
upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic / assumption
summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/07-regression/tobit-regression.Rmd\#for-reviewers}{\texttt{tobit-regression}
--- For Reviewers}

\subsubsection{Truncated Regression}\label{truncated-regression}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Truncated Regression} as implemented in \emph{{[}package
vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list
checks performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Truncated Regression yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/07-regression/truncated-regression.Rmd\#for-reviewers}{\texttt{truncated-regression}
--- For Reviewers}

\subsubsection{Two-Proportion Test}\label{two-proportion-test}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Two-Proportion Test} as implemented in \emph{{[}package vX.Y{]}}
in R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list checks
performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Two-Proportion Test yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/04-inferential/two-proportion-test.Rmd\#for-reviewers}{\texttt{two-proportion-test}
--- For Reviewers}

\subsubsection{Two-Sample t-Test}\label{two-sample-t-test}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Two-Sample t-Test} as implemented in \emph{{[}package vX.Y{]}}
in R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list checks
performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Two-Sample t-Test yielded \emph{{[}estimate{]}} (95\% CI \emph{{[}lower,
upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic / assumption
summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/04-inferential/two-sample-t-test.Rmd\#for-reviewers}{\texttt{two-sample-t-test}
--- For Reviewers}

\subsubsection{Two-Way ANOVA}\label{two-way-anova}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Two-Way ANOVA} as implemented in \emph{{[}package vX.Y{]}} in R
\emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list checks
performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Two-Way ANOVA yielded \emph{{[}estimate{]}} (95\% CI \emph{{[}lower,
upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic / assumption
summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/04-inferential/two-way-anova.Rmd\#for-reviewers}{\texttt{two-way-anova}
--- For Reviewers}

\subsubsection{Type I and Type II
Errors}\label{type-i-and-type-ii-errors}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Type I and Type II Errors} as implemented in \emph{{[}package
vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list
checks performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Type I and Type II Errors yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/04-inferential/type-i-type-ii-errors.Rmd\#for-reviewers}{\texttt{type-i-type-ii-errors}
--- For Reviewers}

\subsubsection{Weighted Log-Rank Tests}\label{weighted-log-rank-tests}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Weighted Log-Rank Tests} as implemented in \emph{{[}package
vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list
checks performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Weighted Log-Rank Tests yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/11-survival/weighted-log-rank.Rmd\#for-reviewers}{\texttt{weighted-log-rank}
--- For Reviewers}

\subsubsection{Welch's t-Test}\label{welchs-t-test}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Welch's t-Test} as implemented in \emph{{[}package vX.Y{]}} in R
\emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list checks
performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Welch's t-Test yielded \emph{{[}estimate{]}} (95\% CI \emph{{[}lower,
upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic / assumption
summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/04-inferential/welch-t-test.Rmd\#for-reviewers}{\texttt{welch-t-test}
--- For Reviewers}

\subsubsection{Wilcoxon Signed-Rank
Test}\label{wilcoxon-signed-rank-test}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Wilcoxon Signed-Rank Test} as implemented in \emph{{[}package
vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list
checks performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Wilcoxon Signed-Rank Test yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/04-inferential/wilcoxon-signed-rank.Rmd\#for-reviewers}{\texttt{wilcoxon-signed-rank}
--- For Reviewers}

\subsubsection{Worked lme4::lmer
Examples}\label{worked-lme4lmer-examples}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Worked lme4::lmer Examples} as implemented in \emph{{[}package
vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list
checks performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Worked lme4::lmer Examples yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/07-regression/lme4-lmer-examples.Rmd\#for-reviewers}{\texttt{lme4-lmer-examples}
--- For Reviewers}

\subsubsection{Zero-Inflated Models}\label{zero-inflated-models}

\textbf{Methods paragraph (copy-paste, fill in italics):}

\begin{quote}
\emph{{[}N={]}} participants/observations were analysed using
\textbf{Zero-Inflated Models} as implemented in \emph{{[}package
vX.Y{]}} in R \emph{{[}vX.Y{]}}. Assumptions were checked: \emph{{[}list
checks performed{]}}. \emph{{[}Effect size with 95\% confidence interval
reported as the primary outcome{]}}. Two-sided p-values use \emph{α =
0.05}; \emph{{[}multiple-comparison adjustment if any{]}}.
\end{quote}

\textbf{Results paragraph (copy-paste):}

\begin{quote}
Zero-Inflated Models yielded \emph{{[}estimate{]}} (95\% CI
\emph{{[}lower, upper{]}}, p = \emph{{[}value{]}}). \emph{{[}Diagnostic
/ assumption summary{]}}. \emph{{[}Sensitivity analysis result{]}}.
\end{quote}

\textbf{Reviewer checklist for this method.} →
\href{../chapters/07-regression/zero-inflated-models.Rmd\#for-reviewers}{\texttt{zero-inflated-models}
--- For Reviewers}

\end{document}
